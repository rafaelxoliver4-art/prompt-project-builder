# PROGRESS — Mobile Price Tracker

> The dated running log + status board. This is the **communication channel** between the Architect
> and Code (via the Bridge). Every working cycle appends here (chronological, newest at the bottom).
> **Standing rule (Bridge):** every CODE TASK ends by updating this file AND CONTEXT.md (durable
> knowledge), committed + pushed — new sessions bootstrap exclusively from these files. See INSTRUCTIONS §2.1.

---

## Status board

| Area | State | Notes |
|------|-------|-------|
| Foundations (root docs) | ✅ done | INSTRUCTIONS / GOVERNANCE / WORKFLOW / READMEs created. |
| Project scaffold | ✅ done | Folder layout, CONTEXT, PROGRESS, config, src skeleton, tests, CI workflow. |
| Reconnaissance | ✅ done | Stacks identified (TIM=Drupal, Claro=Next.js, Vivo=AEM). See CONTEXT §3. |
| Data schema | ✅ done | `Plan` dataclass + stable **`plan_id`** (CONTEXT §6 / §4 decision); canonical key = (carrier, state, plan_id). |
| Excel writer | ✅ done (offline) | Works on fixture data; verified by demo run + unit test. |
| Adapters (vivo/claro/tim) | ✅ all three live | **Claro** (`__NEXT_DATA__`), **Vivo** (Playwright DOM), **TIM** (Drupal `ofertas` JSON) parse real SP plans. |
| Live scraping | ✅ all sources live | **Mobile** claro 19 + vivo 26 + tim 19 = **64** SP plans; **convergent** tim 3 + vivo 11 + claro 10 = **24** combos (2026-08-10). Claro cross-sell cards are attributed to the line their **slug** declares, not the page they sit on (#39). |
| Scheduling (GitHub Actions) | ✅ LIVE | Daily **18:00 BRT** (`0 21 * * *`); first snapshot 2026-06-22. **49/50 days captured (98 %)** through 2026-08-10 — **2026-08-04 is the only permanent loss** (#38). Green every night since the CI decoupling; timestamps read ~18:30 BRT, confirming the #38 timezone fix in production. |
| Data-loss resistance | ✅ structural (#41) | **Four collection passes per São Paulo day** (18:00 + three catch-ups), each scraping only what is missing and no-oping on a healthy day. Fail-fast timeouts on every job + retried/bounded browser install. Watchdog after the last pass. |
| Partial-day resilience | ✅ done (#40) | A carrier that REFUSES us (403/CAPTCHA) no longer costs the carriers that answered — the day commits with a recorded gap + `CARRIER UNAVAILABLE` e-mail. Convergent can no longer block mobile. A 21:00 BRT catch-up pass retries only what is missing. |
| Snapshot dating | ✅ fixed (#38) | `snapshot_date` is the **São Paulo** day (`project.timezone`), not the UTC runner's day — the two agree at 18:00 BRT, so the bug hid for 44 snapshots and only showed on an after-21:00-BRT run. |
| Unattended-run safety | ✅ done (#37/#38) | **Sanity guardrail blocks** a degraded commit + e-mails; **staleness alert** at run start; **watchdog workflow** 20:00 BRT (date anchored to its scheduled hour, #38); transient-only retry. |
| CI ≠ data collection | ✅ done (#38) | `tests` and `collect` are **independent jobs** — a red test can no longer stop the scrape (proven live). Bad scrapes still block the commit (also proven). Red tests stay loud: visible failure + `CI TESTS FAILING` e-mail. |
| Build phase | ✅ CLOSED (#38) | All 🔴 P0 + 🟠 P1 audit findings resolved. Project is in **steady-state operation**; 🟡 P2 list is open + explicitly optional. |
| GitHub + Drive backup | 🟡 GitHub done; Drive pending | Committed daily history begun + **weekly Monday backup** into `backups/`. Google Drive mirror = fast-follow (rclone stub in workflow). |

Legend: ✅ done · 🟡 in progress / partial · 🔌 not started · ⛔ blocked

---

## Next actions (Architect's recommendation)
1. **Bridge:** create the GitHub repo and push this scaffold (CODE TASK #1).
2. **Code:** set up the Python env, install deps, run the offline demo + tests to prove the pipeline (CODE TASK #2).
3. **Code:** implement the **Claro** adapter first (cleanest — `__NEXT_DATA__` JSON), report a sample of real SP plans (CODE TASK #3).
4. Then Vivo (AEM `.model.json` probe), then TIM (HTML + image-price handling).
5. Wire GitHub Actions to run daily and commit the Excel; then add the Drive mirror.

---

## Log

### 2026-06-11 — Project initialized (Architect)
- Created the builder repo and the four root docs (INSTRUCTIONS, GOVERNANCE, WORKFLOW, README).
- Stood up the `mobile-price-tracker` project: CONTEXT, PROGRESS, `config/sources.yaml`, `src/mobile_tracker/*`, tests, and a GitHub Actions workflow.
- Ran reconnaissance on all three carriers via `web_fetch`; recorded stacks, data-access strategy, and gotchas in CONTEXT §3.
- Defined the `Plan` schema and the 4-sheet Excel layout; implemented `excel_writer.py` and a `--demo` mode in `main.py` seeded with sample data (incl. the real Vivo *60GB/R$150* offer found in recon) so the pipeline is verifiable offline.
- Wrote adapter **stubs** with documented per-carrier strategies; real parsing is deferred to Code on the live sites (sandbox is network-restricted).
- **Open questions for the Bridge:** see CONTEXT §10.
- **Handed off:** CODE TASK #1 (init repo) and #2 (env + verify pipeline).

### 2026-06-11 — Excel formula fix + offline verification (Architect)
- **Bug:** on LibreOffice recalc, the summary sheet's `MINIFS`/`MAXIFS` returned the `IFERROR` fallback (`"-"`) while `AVERAGEIFS`/`COUNTIF` worked.
- **Cause:** `MINIFS`/`MAXIFS` are post-2007 functions and must be stored with the `_xlfn.` prefix to be recognized by the OOXML engine; the pre-2007 functions don't need it.
- **Fix:** `excel_writer.py` now writes `=IFERROR(_xlfn.MINIFS(...),"-")` and `=IFERROR(_xlfn.MAXIFS(...),"-")`.
- **Verified:** rebuilt 3 synthetic snapshots (2026-06-09/10/11), recalced with LibreOffice → **0 formula errors, 12 formulas**; summary now computes real values (e.g. vivo min 14.99 / max 139, claro 59.99 / 119.99, tim 49.99 / 139.99). `pytest` → **6 passed**.
- **Status:** offline pipeline (schema → writer → 4 sheets → recalc) is green and idempotent. Ready to hand to Code.

### 2026-06-11 — Bridge decisions recorded (Architect)
- Resolved all four open questions; CONTEXT bumped to **v0.2.0** (§10 + §7/§8 + changelog).
  1. **Excel layout:** per-carrier sheets + dashboard = **backlog**, build after live pipeline works; keep interim function-sheets.
  2. **Plan depth:** **full detail** — adapters expand "ver mais"/modals. Noted in each adapter docstring + §8 (Playwright-favored, longer runs).
  3. **Drive mirror:** **rclone inside GitHub Actions** (runs unattended; desktop-sync needs machine on). One-time local `rclone config` by the user → `rclone.conf` stored as a GitHub Secret → `rclone copy` step after commit.
  4. **History:** keep all snapshots forever.
- `pytest` still **6 passed**; edited adapters parse clean.
- Unchanged: CODE TASK #1 (init repo) and #2 (offline verify) are unaffected by these choices and ready to run now. The decisions shape CODE TASK #3+ (live adapters) and the later Actions/Drive wiring.

### 2026-06-11 — CODE TASK #1 done: repo initialized and pushed (Code)
- Repo initialized and pushed: **https://github.com/rafaelxoliver4-art/prompt-project-builder** (private, branch `main`, root commit `463c0d3`). INSTRUCTIONS → **v1.1.0** (Code-writes-all-files role split; §6 "Where work is saved" added). Offline pipeline verified earlier by the Architect — 6 tests passing.
- **Caveat for the Actions wiring task:** `.github/workflows/mobile-price-tracker.yml` is **excluded from the push** (still on disk, untracked). The machine's `gh` OAuth token lacks the `workflow` scope, and GitHub rejects any push that adds a workflow file without it. Before wiring CI: run `gh auth refresh -h github.com -s workflow` (Bridge completes the device-code + email verification on github.com), then commit and push the workflow file.
- Repo-local git identity set (`rafaelxoliver4-art` / `<your-alert-recipient>`); no global config touched. Verified no secrets staged (`.env`/`*.key`/`rclone.conf` excluded by `.gitignore`).

### 2026-06-11 — CODE TASK #2 done: offline pipeline verified on the real machine (Code)
- Environment: **Windows 11 Pro (10.0.26200), Python 3.13.13**, venv at `.venv/`, all `requirements.txt` deps installed, Playwright **Chromium installed** (ready for live tasks).
- `python -m pytest -q` → **6 passed** (17.50s).
- `python -m mobile_tracker.main --demo` → `Wrote data\mobile_plans.xlsx: 12 plans in latest (2026-06-11), 3 snapshot(s), 5 change(s).` (Note: `python -m` needs `PYTHONPATH=src` — pyproject only wires `src/` for pytest; consider `pip install -e .` or a runner script in a future task.)
- Workbook verified in **real Excel (COM recalc)**: 4 sheets (`history`/`latest`/`changes`/`summary`); summary KPIs compute real values — vivo 4 plans min R$ 14.99 / avg R$ 92.25 / max R$ 150.00; claro 4 plans 59.99 / 83.74 / 119.99; tim 4 plans 30.00 / 74.99 / 139.99. No `"-"` fallbacks.
- Demo-overwritten `data/mobile_plans.xlsx` restored via `git checkout --` (not committed, per task guardrail). **Offline pipeline verified on the real machine.**

### 2026-06-11 — Bridge standing rule: two-files closing ritual (Code, on Bridge's instruction)
- Rafael's rule, now codified: **every CODE TASK ends with BOTH PROGRESS.md (dated entry) and CONTEXT.md (durable knowledge) updated, committed, and pushed.** Rationale: new chat/Code sessions bootstrap exclusively from these files — anything not written here is lost.
- Written into INSTRUCTIONS **v1.2.0** (§2.1 "Closing ritual" + changelog) and into both files' headers. CONTEXT bumped to **0.2.2**.
- Also flagged by the Bridge: the prices in the workbook are **demo sample data, not real prices** (see CONTEXT §5 note); Bridge is raising it with the Architect. Real prices arrive with the live adapters (CODE TASK #3+).

### 2026-06-11 — Backup to Google Drive + machine transfer prep (Code)
- Bridge is moving to another computer; asked to ensure everything is saved to GitHub **and** Google Drive.
- **GitHub:** verified fully pushed — working tree clean, `origin/main` at `0587ba9` (only untracked item is `.github/`, the workflow file that can't be pushed yet — workflow-scope auth pending).
- **Google Drive:** copied the full project to **`G:\Meu Drive\prompt-project-builder`** (account `<your-alert-recipient>`, Drive Desktop running) via `robocopy /E` excluding `.venv`/`__pycache__`/`.pytest_cache`. The Drive copy **includes `.git` history AND `.github/workflows/mobile-price-tracker.yml`** — so it is the only cloud copy that has the workflow file. ~0.2 MB; all key files verified present.
- Recorded the three-copy map + new-machine bootstrap steps in **CONTEXT §5** (v0.2.3). Key gotcha for the new machine: a fresh `git clone` will be **missing the `.github/` workflow file** — restore it from the Drive backup.
- ⚠️ Drive Desktop syncs the local `G:\Meu Drive` folder to the cloud in the background; allow a moment for upload to finish before powering down this machine.

### 2026-06-17 — CODE TASK #2.5: transfer verified on machine #2 (Code)
- **Machine:** Windows 11 **Home** Single Language (10.0.26200), **Python 3.12.10**, user `rafae`. Project arrived via **OneDrive sync**, not a fresh clone — so `.git` history and the `.github/` workflow file came across together. Working copy: `C:\Users\rafae\OneDrive\Área de Trabalho\prompt-project-builder`.
- **Git verification — identical to GitHub:** `git fetch` OK; local `HEAD == origin/main == 0f557e4` ("docs: backup-to-Drive…"); `git diff origin/main` → **no differences** (tracked tree byte-identical); ahead/behind `0 0`; only untracked item is `.github/`. (`git fsck` flagged a stale reflog entry + dangling objects — harmless residue of the folder-copy; committed history intact.)
- **Workflow file:** **already present** (no restore needed) — OneDrive carried it. Byte-identical (SHA256) to the copy under the sibling `…\CFA\prompt-project-builder\` folder.
- **Google Drive backup:** **could not be verified from this machine** — Google Drive Desktop is **not installed** here (no `DriveFS`, no mounted drive). The cloud mirror (`<your-alert-recipient>`) presumably remains intact, untouched by the transfer; verify it later from a machine with Drive Desktop, or treat OneDrive+GitHub as sufficient redundancy.
- **⛔ Env rebuild + smoke test DEFERRED (network blocker).** Removed the stale OneDrive `.venv` (Python 3.13 from machine #1) and created a fresh Python-3.12.10 venv, but **`pip install` cannot reach PyPI**. Diagnosed: active **McAfee VPN** (MTU 1420, prioritized) is an **MTU/PMTUD black hole** — large packets to Fastly IPs (`151.101.x.x`: pypi.org, files.pythonhosted.org) are dropped (`ping -f -l 1472` → "needs fragmentation, DF set"), small packets reply. github.com/example.com fine. **So `pytest` and `--demo` were not run on this machine.** Fix (Bridge): disconnect McAfee VPN or clamp MTU, then `pip install -r requirements.txt` + `playwright install chromium` + re-run the offline smoke test (it already passed on machine #1).
- **Decision (Bridge, 2026-06-17):** git proves this copy is identical to GitHub → **continue the project from this machine.** Env/smoke re-verification deferred until the network is sorted.
- **Net:** transfer verified on the new machine (working copy == GitHub; workflow file present). Outstanding: rebuild env once McAfee VPN MTU issue is resolved; verify the Drive mirror from a Drive-enabled machine.

### 2026-06-17 — Cross-check with machine #1 Code (transfer Q&A, via Bridge)
Machine #2 sent three open questions to the still-running machine #1 Code instance; Bridge relayed the answers. Results:
- **Q1 — Google Drive mirror: CONFIRMED intact by machine #1.** The `robocopy` to `G:\Meu Drive\prompt-project-builder` finished successfully and `.github/workflows/mobile-price-tracker.yml` **is present inside the Drive copy** (re-verified on machine #1, still powered on). Caveat: a definitive "Drive Desktop finished uploading to cloud" signal only exists in the tray UI, not programmatically — but files are fully materialized locally (not dehydrated). **Moot for machine #2 anyway**, which got the project via OneDrive (already has `.git` + workflow file), so it is not dependent on the Drive copy.
- **Q2 — Local-only / gitignored files: NONE needed.** Machine #1 scan confirmed the only untracked file is `.github/workflows/mobile-price-tracker.yml` (already present on machine #2); ignored items are only regenerable caches (`.venv/`, `.pytest_cache/`, `__pycache__/`). **No `.env`, no `rclone.conf`, no `*.key`/`*.secret`, no credentials** anywhere in the tree (confirmed absent). Secrets will live in GitHub Secrets when wired.
- **Q3 — `gh` workflow-scope auth: NOT done, and per-machine.** Machine #1's token still has `gist, read:org, repo` (no `workflow`); workflow file is still **not on GitHub** (`origin/main == e8deb47`). `gh` auth is per-machine, so machine #2 must run `gh auth refresh -h github.com -s workflow` itself (device-code + email verification) before it can `git add .github/ && commit && push` the workflow file to land it on GitHub.
- **⚠️ NEW HAZARD — shared `.git` over cloud sync.** The `.git` folder lives inside the OneDrive-synced directory, and BOTH machines now sync the same folder. Machine #1 observed its local HEAD jump `0f557e4 → e8deb47` with no git command — OneDrive overwrote `.git` from machine #2's version. Currently consistent (both at `e8deb47`), but a cloud-synced `.git` written by two active machines can **corrupt the repo or create silent divergence** (`…-conflicted copy…` files in `.git/`). **Rule going forward: work on ONE machine at a time and sync between them via GitHub (`push`/`pull`), NOT by letting OneDrive/Drive replicate `.git`.** Since machine #2 is the new home, machine #1 should stop committing in this folder.
- **⚠️ Workflow bug confirmed (machine #1).** The CI workflow's **"Run tracker"** step calls `python -m mobile_tracker.main` **without `PYTHONPATH=src`**, so the first CI run will hit the same `ModuleNotFoundError` documented in CONTEXT §5. Verified on machine #2: no `PYTHONPATH` anywhere in `.github/`. Fix before the first Actions run — add `env: PYTHONPATH: src` to the step, or `pip install -e .` in the install step.
- **Note (machine #2):** a stray non-git duplicate of the project also exists at `…\Área de Trabalho\CFA\prompt-project-builder\` (no `.git`; identical workflow file). Harmless, but a candidate for cleanup to avoid confusion.
- **Net:** all three questions resolved; transfer fully corroborated from both ends. Two action items recorded for later: (a) the `.git`-over-cloud-sync hazard → use GitHub to sync, not OneDrive; (b) the workflow `PYTHONPATH=src` fix before wiring Actions.

### 2026-06-19 — Single-machine consolidation + env rebuilt + smoke test GREEN (Code)
- **Bridge clarification:** there is only **one** working machine — this one (user `Rafael`, Windows 11 Pro, **Python 3.13.14**). The machine-#1/#2 transfer track is set aside; CONTEXT §5's machine-#2 sections are now marked historical. Future PC migration will go via GitHub `git clone`, not folder sync. Working in place under OneDrive for now (single writer → no `.git` collision risk); the OneDrive relocation is deferred to the future PC change.
- **Env rebuilt (the deferred #2.7 work, done here):** removed the foreign OneDrive-synced `.venv` (the `rafae`/Python-3.12.10 one — wrong interpreter for this machine), created a fresh venv on **Python 3.13.14**. `pip install -r requirements.txt` → **completed cleanly** (exit 0; no PyPI/VPN blocker on this machine — the McAfee-VPN MTU issue was machine-#2-only). `playwright install chromium` → done.
- **Offline smoke test GREEN:** `$env:PYTHONPATH="src"; pytest -q` → **6 passed**. `--demo` → `Wrote data\mobile_plans.xlsx: 3 plans in latest (2026-06-19), 4 snapshot(s), 12 change(s).` Excel COM recalc confirmed 4 sheets + real summary KPIs (vivo R$150.00 / claro R$64.99 / tim R$30.00 — one demo plan per carrier). Demo workbook restored via `git checkout --` (not committed).
- **Still outstanding (unchanged):** `.github/workflows/mobile-price-tracker.yml` remains un-pushed (this machine's `gh` token still lacks `workflow` scope); CI workflow needs the `PYTHONPATH=src` fix before the first Actions run. Both deferred to the Actions-wiring task.
- **Next:** CODE TASK #3 — implement the Claro adapter (first live data via `__NEXT_DATA__`).

### 2026-06-19 — CODE TASK #3: Claro adapter live — FIRST REAL DATA (Code)
- **Editable install:** `pip install -e .` — `python -m mobile_tracker.main` now runs with **no `PYTHONPATH`** (kills the recurring quirk and the CI "Run tracker" bug). `pyproject.toml` already had the packaging config; no change needed.
- **Adapter** (`adapters/claro.py`): split into a **pure** `parse_next_data(data, target)` (offline-testable) + a polite `fetch()` (one `httpx` GET/page, real UA, 2–6s random delay, single retry, raw capture saved to `data/raw/`). Parses the Storyblok `__NEXT_DATA__` tree (`dynamicComponents.body[] → card_360 → data.data[]`). **No Playwright needed** — modal detail is already embedded (§3 documents the full JSON path + the two price formats).
- **Live run** `python -m mobile_tracker.main --only claro` → `Collected 13 valid plans across 4 targets. claro: 13`. (Exit code 3 is the expected vivo/tim-zero alert from a single-carrier run, not a failure.)
  - **postpaid (5):** Pós 60GB R$124,90 · Pós 50GB com GeForce NOW R$164,90 · Pós 100GB R$179,90 · Pós 150GB R$239,90 · Pós 200GB R$339,90
  - **control (4):** Controle 20GB R$44,90 · Controle 25GB R$59,90 (promo R$54,90) · Controle 30GB R$69,90 · Controle 30GB R$99,90
  - **flex (4):** Flex 15GB R$44,90 · Flex 20GB R$59,90 · Flex 30GB R$69,90 · Flex 40GB R$119,90
- **Tests:** added `tests/test_claro.py` (5 tests) against a trimmed 44KB real-capture fixture (`tests/fixtures/claro_pos_sp.json`) covering field mapping, the slug-name fallback, promo pricing, BRL parsing, and dedup. Full suite **6 → 12 passed**.
- **Findings / deferred:** (a) **Claro prepaid (Prezão)** uses a different layout (`card`/`tab_select`, no `card_360`) → 0 plans today; needs its own parser. (b) Two `Controle 30GB` tiers share a name at different prices — both kept in `history`; `latest` (keyed by name) collapses them. (c) No anti-bot wall encountered — plain GET returned full `__NEXT_DATA__`.
- Workbook restored after the live run (not committed). Raw captures stay gitignored.
- **Next (Architect):** Vivo adapter (AEM `.model.json` probe) or TIM (HTML + SVG-price), and optionally a Claro-prepaid parser.

### 2026-06-19 — CODE TASK #4: Vivo adapter live — second carrier (Code)
- **Fetch path (recon):** plain `httpx` GET → **403** (anti-bot challenge, ~6KB, no prices); `…<page>.model.json` → **also 403**; no usable embedded JSON. A real **headless Chromium (Playwright)** → **HTTP 200**, full ~1MB render, **no CAPTCHA shown**. → Vivo uses **Playwright + DOM scrape** (the sanctioned §10.2 fallback — a real browser, not evasion). Documented in CONTEXT §3.
- **Adapter** (`adapters/vivo.py`): pure `parse_vivo_html(html, target)` (selectolax scrape of `.unique-card`: name `.unique-card__plan`, price `.total-card-price-value`, data `.unique-card__header-benefit`) + Playwright `fetch()` (one load/page, cookie-banner dismiss, polite delay, raw `.html` capture). Mirrors the Claro split.
- **Live run** `python -m mobile_tracker.main --only vivo` → `Collected 24 valid plans across 4 targets. vivo: 24` (exit 3 = expected claro/tim zero-alert). **All four categories parse with one selector set:**
  - **postpaid (7):** Vivo Pós com Amazon R$150 · Globoplay R$165 · Spotify R$165 · Premiere R$180 · Netflix R$180 · Disney+ R$180 · Travel R$215 (70GB)
  - **control (8):** Vivo Controle R$59 · R$80 · Saúde/Educação/Vantagens R$90 · Música/Netflix R$95 · Entretenimento R$110
  - **lite (5):** Easy Lite Anual R$20 · Easy Lite R$30/R$40/R$35/R$50
  - **prepaid (4):** Vivo Pré R$17–R$30
- **Tests:** added `tests/test_vivo.py` (5 tests) against a trimmed 132KB real Playwright-render fixture (`tests/fixtures/vivo_postpaid_sp.html`). Full suite **12 → 17 passed**.
- **Blocks:** none (Playwright passed cleanly; no CAPTCHA). httpx 403 is documented, not evaded.
- **Backlog recorded (per Bridge):** CONTEXT §7 extended with deferred product items — (a) per-operator sheets + cross-operator "comparable plans"; (b) promotions view (promo-vs-regular over time); (c) formatted dashboard — under a **data-correctness/coverage-first** rule. Logged data-quality items: Claro-prepaid parser, and plan-name uniqueness → a stable `plan_id` (schema change, **needs Bridge YES**).
- Workbook restored after the live run (not committed). Raw captures stay gitignored.
- **Next (Architect):** TIM adapter (Drupal HTML + SVG-embedded prices, state in URL path), and/or the deferred Claro-prepaid parser / `plan_id` schema decision.

### 2026-06-19 — CODE TASK #5: TIM adapter live — all three carriers now live (Code)
- **Fetch path (recon):** TIM is server-rendered Drupal; plain `httpx` GET → **200** (no anti-bot, no browser). DOM is noisy (Acquia Site Studio, per-instance UUID classes, device sales) with **no clean plan-card class and no `<table>`**. The structured grid is embedded JSON: `<script data-drupal-selector="drupal-settings-json">` → **`settings["ofertas"][]`**. Documented in CONTEXT §3.
- **SVG-price outcome — no OCR needed.** The hero SVG `alt` carries the marketing headline price, but **every per-plan price is a clean JSON text value** (`field_preco_card_oferta`), so OCR was unnecessary (no Bridge ask required). Body-text prices (HBO Max R$20,90 etc.) are add-ons, ignored.
- **Adapter** (`adapters/tim.py`): pure `parse_tim_html(html, target)` (reads the drupal-settings JSON, walks to `ofertas`, maps `field_preco_card_oferta` → price and cleaned `title` → name+GB) + thin `httpx` `fetch()` (real UA, delay, single retry, raw `.html` capture). No Playwright.
- **Live run** `python -m mobile_tracker.main --only tim` → `Collected 15 valid plans across 3 targets. tim: 15` (exit 3 = expected vivo/claro zero-alert).
  - **control (5):** TIM Controle Plus 45GB R$64,99 · TIM Controle 41GB R$58,99 · Premium 53GB R$84,99 · Light Express 31GB R$60,99 · Pro Express R$64,99
  - **postpaid / TIM Black (7):** 70GB R$129,99 · Plus 80GB R$149,99 · Premium 110GB R$159,99 · C Ultra 95GB R$159,99 · A Express 67GB R$119,99 · B Express 82GB R$144,99 · C Express 107GB R$164,99
  - **prepaid / TIM Pré XIP (3):** 6GB R$20 · 8GB R$25 · 16GB R$30 (price = recarga amount)
- **Tests:** added `tests/test_tim.py` (5 tests) against a tiny 1KB fixture holding the real `ofertas` JSON (mapping, title cleaning incl. `[PROD]`/`[On Air]` + dash handling, price parse, dedup). Full suite **17 → 22 passed**.
- **Blocks:** none.
- **Milestone:** **all three carriers now live — 52 real SP plans** (Claro 13 + Vivo 24 + TIM 15). Parked the Bridge's cross-operator **comparison methodology** (within-category, align by price rank) in CONTEXT §7.
- Workbook restored (not committed). Raw captures gitignored.
- **Next (Architect):** the deferred items — Claro-prepaid parser, the `plan_id` schema decision (needs Bridge YES), then wiring the **daily GitHub Actions job** (first committed history) + the per-operator/promotions/dashboard backlog.

### 2026-06-19 — CODE TASK #6: stable plan_id + re-keyed latest/history/changes (Code)
- **Schema change (Bridge-approved):** added `plan_id` to the `Plan` dataclass + `COLUMNS` (after `plan_name`); CONTEXT → **v0.3.0**. `[DECISION 2026-06-19]` recorded in §4: canonical key = (carrier, state, plan_id), **never price-derived**.
- **Per-carrier plan_id (native where available, §3):**
  - **Claro** → plan **slug** (`claro:plano-controle-30gb` vs `claro:plano-controle-30gb-gaming`).
  - **Vivo** → **offer code** from the card HTML (`vivo:VIV202600029270` vs `…300`); `SELF…` fallback.
  - **TIM** → Drupal **`nid`** (`tim:155891`); `field_sku` then a name-slug as fallbacks. (Note: `nid` is a Drupal list-field `[{"value": …}]` — unwrapped via `_field`.)
  - Fallback for any card lacking a native id → `carrier:<slugified plan_name>` (deterministic). Added `slugify()` to `adapters/base.py`.
- **`excel_writer` re-keyed:** history dedup = (snapshot_date, carrier, state, plan_id); `latest` no longer collapses same-named plans; `changes` matches by (carrier, state, plan_id) and now carries a `plan_id` column. Falls back to `plan_name` if `plan_id` is ever missing (so old/demo rows don't crash).
- **Tests (22 → 27):** each adapter test asserts `plan_id` is native + unique; **collision test** proves two "Controle 30GB" with distinct ids both survive in `latest`; **per-plan price-change test** proves a move on one isn't confused with the same-named other. Rebuilt the TIM fixture to include `nid`. All offline.
- **Review run** `python -m mobile_tracker.main` (all 3 carriers) → `Collected 50 valid plans across 11 targets` (claro 13 / tim 15 / vivo 22); **`latest` = 49 distinct rows**, every `plan_id` set + unique. Confirmed the duplicate-named plans are now **separate rows**: Claro "Controle 30GB" ×2 (R$69.9 / R$99.9), Vivo "Vivo Controle" ×2 (R$59 / R$80). (50→49: one Claro flex card cross-listed on two category pages correctly shares its `plan_id`.)
- **Review workbook left at `data/mobile_plans.xlsx` (uncommitted)** for the Bridge to open — fresh single snapshot of real SP data. Not committed (per task). Raw captures gitignored.
- **Next (Architect):** wire the **daily GitHub Actions job** (first committed history; needs the one-time `gh` `workflow`-scope auth + the `PYTHONPATH=src`/editable-install fix in the workflow), then the Claro-prepaid parser and the per-operator / promotions / dashboard backlog.

### 2026-06-22 — CODE TASK #7: data-quality gaps closed (prepaid + promo) (Code)
- **Vivo prepaid: 1 → 4 tiers.** Root cause: the 4 recharge tiers (R$17/20/25/30) all share ONE page-level offer code (`VIV202600022379`) and the name "Vivo Pré", so the #6 `plan_id` collapsed them. Fix: `plan_id = vivo:<code>-<gb>gb` — the data allowance (25/9/5/4 GB; non-price) disambiguates. Vivo total 22 → **24**.
- **Claro prepaid: 0 → 1 (Prezão).** New `parse_claro_prepaid` over the `tab_select` layout (the Prezão page is NOT `card_360`): "Prezão R$1 por dia", price R$1/dia from the tab title, 12GB from the content; recarga tiers (R$15–30) noted in `price_note`, not as separate plans. Deeper Pré tiers deferred (§7). Claro total 13 → **14**.
- **Promo prices wired for all three carriers** (`price_promo_brl` + `price_note`): Claro = `price.prefix` struck regular; Vivo = `.unique-card__price-old`; TIM = `field_preco_adicional_tracejado`. Live coverage right now = **1** (Claro "Controle 25GB" R$59,90→R$54,90); Vivo/TIM mechanism present but no promo on today's fetched pages. Populates automatically when a discount is shown.
- **TIM "Controle Pro Express" `data_gb`:** left **null** — its allowance isn't in the structured `ofertas` fields (only a "500 MEGA" bonus highlight) and the title has no GB token; documented in §3 rather than inferring a misleading value.
- **Tests 27 → 30:** Vivo prepaid 4-distinct-tier test, Vivo promo (struck-price) test, Claro Prezão test. Added trimmed real fixtures `vivo_prepaid_sp.html` + `claro_prepaid_sp.json`. All offline.
- **Review run** `python -m mobile_tracker.main` → `Collected 53 valid plans` (claro 14 / tim 15 / vivo 24); **52 distinct in `latest`**, every `plan_id` unique. Vivo prepaid shows all 4 tiers; Claro prepaid present. (Bonus: `plan_id` keying also collapsed the cross-listed Claro flex card correctly — control now shows its true 3.) CONTEXT → **v0.3.1**.
- **Review workbook left at `data/mobile_plans.xlsx` (uncommitted)** for the Bridge. Not committed; raw captures gitignored.
- **Next (Architect):** the dataset is complete + correct → wire the **daily GitHub Actions job** (first committed history; needs the one-time `gh workflow`-scope auth + the editable-install/`PYTHONPATH` fix in the workflow), then the per-operator / promotions / dashboard backlog.

### 2026-06-22 — CODE TASK #9: cross-operator comparison sheet (Code)
- Added the **`comparison`** sheet (presentation-only; adapters/schema/scraping untouched). Implements the Bridge's methodology (CONTEXT §7, moved from parking-lot to built): compare like-for-like **within each category**, aligned by **price rank**.
- **Groups → category:** Pure Postpaid=`postpaid`, Control/Hybrid=`control`, Prepaid=`prepaid`, Digital={`lite`,`flex`}. Each carrier's plans sorted ascending by `price_brl`, aligned across Vivo/Claro/TIM by rank; `price_promo_brl` shown inline when present (not re-ranked). Layout: `Rank | Vivo R$ | Claro R$ | TIM R$ | Vivo/Claro/TIM plan (XGB)`.
- **Code:** `build_comparison_data` (pure, offline-testable) + `_write_comparison` in `excel_writer.py`, wired after `latest`. `history`/`latest`/`changes`/`summary` untouched (existing-sheets test updated to expect 5 sheets).
- **Review run** (`python -m mobile_tracker.main`, 53 plans / 52 latest) → comparison sheet built with per-group max ranks: **Pure Postpaid 7, Control/Hybrid 8, Prepaid 4, Digital 5.** Rank-1 postpaid row = Vivo R$150 / Claro R$124,90 / TIM R$119,99 (each carrier's cheapest). Caveats print in-sheet (prepaid unit mismatch; Digital = Vivo Lite + Claro Flex, TIM none). Claro Controle 25GB shows its promo inline.
- **Tests 30 → 35:** ascending-per-carrier + rank-alignment, Digital pulls lite+flex (TIM empty), prepaid caveat present, four groups in order, write_workbook emits `comparison` without disturbing the others. All offline.
- **Review workbook left at `data/mobile_plans.xlsx` (uncommitted)** for the Bridge — open the `comparison` tab. **First cut — expect layout iteration.** CONTEXT → **v0.3.2**.
- **Next (Architect):** react to the comparison layout; then the **daily GitHub Actions job** (committed history) + the promotions-over-time view and dashboard.

### 2026-06-22 — CODE TASK #10: per-operator sheets (one tab per carrier) (Code)
- Added **one sheet per carrier** present in the latest snapshot (Vivo / Claro / TIM; absent carriers get none, so it scales). Presentation-only — adapters/schema/scraping untouched. §7 per-operator item moved backlog → built.
- **Structure:** within each sheet, plans **grouped by category** (Postpaid → Control → Digital {lite,flex} → Prepaid) with a bold sub-header, **sorted ascending by `price_brl`**. Readable column set: **Plan | Price R$ | Promo R$ | Data | Voice | Unlimited apps | Streaming | Notes** (Data = `data_gb`+`data_note`; Notes = `extra_benefits`+`price_note`; `plan_id` omitted; blank where null).
- **Code:** `build_operator_sheets` (pure, offline-testable) + `_write_operator_sheets` in `excel_writer.py`, wired after the comparison sheet. The five prior sheets (history/latest/changes/summary/comparison) untouched → **8 sheets total**.
- **Review run** (53 plans / 52 latest) → tabs built: **Vivo** 7+8+5+4, **Claro** (postpaid/control/flex/prepaid), **TIM** (postpaid/control/prepaid). Vivo sheet verified: Postpaid 150→215, Control 59→110, Digital 30→50, Prepaid 17→30, streaming + notes populated.
- **Tests 35 → 40:** one-sheet-per-present-carrier (TIM absent → no sheet), blocks grouped-in-order + price-sorted, only that carrier's plans, the 8-column set + promo/data formatting, write_workbook emits carrier sheets without disturbing the others. All offline.
- **Review workbook left at `data/mobile_plans.xlsx` (uncommitted)** — open the Vivo/Claro/TIM tabs. **First cut — expect iteration.** CONTEXT → **v0.3.3**.
- **Next (Architect):** react to the by-operator + comparison layouts; then the **daily GitHub Actions job** (committed history) and the promotions-over-time view / dashboard.

### 2026-06-22 — CODE TASK #8: daily Actions job LIVE — committed price history begins 🎉 (Code)
- **`gh workflow` scope authorized** (Bridge ran `gh auth refresh -s workflow`, device-code). Workflow file **pushed + live on GitHub** (`f37562e`) after being un-pushable since Task #1.
- **Workflow fixed:** `pip install -e .` (editable install) → `python -m mobile_tracker.main` runs with **no `PYTHONPATH`** (kills the documented CI bug); `playwright install --with-deps chromium`; offline `pytest` gate; `permissions: contents: write`; commits `data/mobile_plans.xlsx` after the run. **Daily cron `0 21 * * *` = 18:00 BRT** + `workflow_dispatch` (default `live`).
- **Synthetic seed cleared** (`25f0da6`) — append-only history now starts empty and fills with real snapshots.
- **PROVED end-to-end:** manual run `gh workflow run` → job green in 2m21s. **"Collected 52 valid plans across 11 targets"** — *all three carriers scraped from GitHub's runner*, including **Vivo via headless Playwright** (the datacenter-IP block risk did **not** materialize). First real snapshot committed by the bot: **`b5ca0a0` — 52 collected / 51 in latest (2026-06-22)**, full 8-sheet workbook. Verified on `origin/main`.
- **Weekly backup wired + fired:** today is Monday (UTC), so the job also committed **`backups/mobile_plans_2026-06-22.xlsx`** (`6bc51f4`) — dated, never overwritten. `backups/.gitkeep` tracks the dir.
- **Daily cron is active** (workflow id `300305398`, status active). Runs unattended at 18:00 BRT going forward.
- **Docs:** §2 cadence → 18:00 BRT; §5 records the live job (auth done, PYTHONPATH fixed, history begun, weekly backup, CI-IP finding = none, working-copy ownership note); §7 parks the monthly price-evolution **heatmap** design target; `sources.yaml` cron updated. CONTEXT → **v0.4.0**.
- **No secrets committed; no Drive/rclone this task** (fast-follow). **Working-copy note:** `data/mobile_plans.xlsx` is now owned by the daily job — pull before working; don't hand-commit local review-run changes to it.
- **Next (Architect):** optionally the **Google Drive mirror** (rclone-in-Actions, needs a Bridge YES + `RCLONE_CONFIG` secret); the promotions-over-time view; and — once history accumulates — the parked monthly price-evolution heatmap + a formatted dashboard.

### 2026-06-22 — CODE TASK #11: visual polish pass (Code)
- **Presentation only** — adapters/schema/scraping/data untouched. All styling is **in code** (`excel_writer.py`) via a reusable design system, so the daily job reproduces the look every run. **No buttons/macros/.xlsm** (Bridge's call — plain `.xlsx` visual styling).
- **Applied to all 8 sheets:** gridlines **OFF** everywhere; navy header + white-bold + thin bottom border; **row banding** `#F2F5FA`; **branded tab colors** (Vivo purple / Claro red / TIM blue / comparison green / summary navy / engine sheets grey); carrier-sheet **category sub-header** accent `#D6E0F0`; right-aligned numbers; frozen header rows.
- **Conditional formatting:** `comparison` color-grades the three R$ columns green→yellow→red **and** highlights the **cheapest carrier per rank** (`#C6EFCE`/`#006100`, stop-if-true so it wins over the scale); `latest` + each carrier sheet color-scale the **price column** (per category block); **promo** cells get the amber `#FFF2CC` accent.
- **Price validation (§8):** cross-checked the comparison vs carriers' official pages + independent sites — cheapest-postpaid ranking + absolute prices **matched** (TIM ≈R$119,90 < Claro R$124,90 < Vivo R$150). Parked comparison refinements: (i) fidelity vs boleto pricing, (ii) consistent GB definition, (iii) prepaid → monthly normalization, (iv) R$/GB value lens.
- **Tests 40 → 45:** gridlines-off on every sheet, tab colors, CF rules on comparison + carrier + latest sheets, banded data rows, sub-header accent. Existing data-integrity tests stay green. CONTEXT → **v0.4.1** (§7 design system, §8 validation, §5 note).
- **Styled review workbook** regenerated locally (live run), left at `data/mobile_plans.xlsx` **uncommitted** for review; the committed copy picks up the styling automatically from the next 18:00 BRT run. Not committed.
- **Next (Architect):** react to the styling (first cut); the parked comparison refinements (value lens / fidelity); Drive mirror; promotions-over-time + heatmap/dashboard as history accumulates.

### 2026-06-22 — CODE TASK #12: comparison → price-evolution matrix (formula-driven) (Code)
- **`comparison` is now the EVOLUTION MATRIX:** `Date` (rows, one per distinct `snapshot_date`, chronological) × four category groups (**Control / Post / Pre / Digital**), each merged over **TIM | Vivo | Claro**. Every value cell is a **live `_xlfn.MINIFS` formula over the `history` sheet** = the cheapest R$ that carrier offered in that category on that date → the workbook is **self-connected** and the matrix grows automatically as daily history accumulates. (Mirrors the `summary` sheet's formula pattern.)
- **Heatmap:** per-group color-scale (green cheap → red dear) over the value cells; 0/no-match → blank so it's ignored. **Digital** = `MIN(lite, flex)` via a reciprocal-max trick (Vivo Lite / Claro Flex; TIM blank). **Pre** = cheapest recharge amount (true R$/day pending validity-days, §8). In-sheet note explains Pre + Digital + the daily→monthly plan.
- **Rank-aligned cross-section preserved** on a new **`Ranking`** sheet (same `build_comparison_data`, renamed `_write_ranking`) so the validated view isn't lost.
- **Same-day idempotency FIXED** (`_merge_history`): a re-run of a `snapshot_date` now **replaces** that date's rows instead of unioning — kills the local re-run inflation flagged in #11 (verified: re-running a date → 1 set of rows, other dates untouched). Presentation/formula + this small merge fix only — adapters/schema/scraping untouched.
- **Review run** (44 plans; one transient `vivo/control` `ERR_NETWORK_CHANGED` WiFi blip — caught, not a block) → matrix built with the 2026-06-22 row, live formulas, 4-group heatmap, gridlines off; `Ranking` present. History has 1 date now → matrix shows 1 row, ready to grow.
- **Tests 45 → 51:** matrix structure (Date + grouped TIM/Vivo/Claro), value cells are `MINIFS`/`history!` formulas, one row per distinct date, heatmap CF rule, `Ranking` preserved, `evolution_dates` helper, and same-day-replace idempotency. Existing tests green. CONTEXT → **v0.4.2**.
- **Review workbook** left at `data/mobile_plans.xlsx` (uncommitted); committed copy gets the matrix from the next 18:00 BRT run. Not committed.
- **Next (Architect):** the **monthly roll-up** of the matrix (rows = month) to match the Figure-5 reference once days accumulate; the value-lens/fidelity refinements; Drive mirror.

### 2026-06-22 — CODE TASK #13: price-evolution line charts (Code)
- Added **four per-category line charts** (Control / Post / Pre / Digital) in a **2×2 block at the top of the `comparison` sheet** (matrix moved below them, header now row 35). Presentation only — adapters/schema/scraping/data untouched.
- **Each chart:** X-axis = the matrix Date column; **3 series = that group's TIM/Vivo/Claro matrix columns**, palette-matched line colors (TIM `#0033A0` / Vivo `#660099` / Claro `#DA291C`); markers on (so the single current point is visible); legend bottom; Y = "R$". Data references rebuilt **each run** to match the current date range (no stale/over-wide ranges).
- **Self-connected:** charts read the matrix → matrix reads `history`, so they **auto-fill into trend lines** as the daily job adds dates. openpyxl native `LineChart` (no images/macros).
- **Verified** (zip-inspected the chart XML, since openpyxl doesn't read charts back): 4 chart parts, each with 3 series referencing `'comparison'!$<col>` matrix columns + the `$A` Date column, and the palette line colors present.
- **Sparsity note (expected):** history has ~1 date now → each series shows one marker until more days accrue. **Charts render values when opened in Excel** (recalcs the matrix formulas); a populated GitHub *preview* would need a headless-recalc step in CI (flagged, out of scope).
- **Tests 51 → 52:** 4 LineChart parts on comparison, 3 series each referencing the matrix columns + Date, palette colors; existing matrix/Ranking/idempotency tests updated for the new matrix row offset and green. CONTEXT → **v0.4.3**.
- **Review copy** left at `data/mobile_plans.xlsx` (uncommitted); committed copy gets charts from the next 18:00 BRT run. Not committed.
- **Next (Architect):** monthly roll-up of the matrix/charts (Figure-5); value-lens/fidelity refinements; optional CI headless-recalc for GitHub-previewable charts; Drive mirror.

### 2026-06-22 — CODE TASK #14: production-readiness audit — set for 18:00 BRT ✅ (Code)
- Verification only (no code changes). Confirmed against the **remote** state (the runner uses `origin/main`, not local).
- **A. Sync — PASS:** tree clean; `HEAD == origin/main == 3d4f0ef`; **no unpushed commits**.
- **B. Workflow config — PASS:** file on `origin/main` (94 lines) with cron `0 21 * * *` (18:00 BRT), `workflow_dispatch`, `contents: write`, `pip install -e .` + `playwright install --with-deps chromium`, `python -m mobile_tracker.main` (no PYTHONPATH), commit-back, weekly-Monday backup; rclone/Drive step commented (won't fail). Default branch = `main`.
- **C. Enabled — PASS:** `gh workflow list` → **active** (id 300305398); prior run succeeded; no cron run yet today (before 21:00 UTC — expected).
- **D. Code current — PASS:** `pytest` → **52 passed**; pyproject editable-install config present; matrix/chart/operator/Ranking functions all on `origin/main`.
- **E. End-to-end proof — PASS:** manual `gh workflow run` → **success in 2m29s**. **Collected 52** (claro 14 / tim 15 / vivo 23), all three scraped, **no block/CAPTCHA/zero**. Committed **51 in latest, 1 snapshot** — idempotent replace confirmed *on the runner* (didn't duplicate today's date). Pulled workbook: 9 sheets, comparison = live-MINIFS matrix + 4 charts, Ranking + per-operator, gridlines off; weekly backup refreshed.
- **BOTTOM LINE: YES — the cron will fire at 18:00 BRT (21:00 UTC) today from `main`**, workflow enabled, latest code live, manual run succeeded with full counts, history idempotent.
- **Open item:** no live-output *sanity* check yet beyond the zero-guard (implausible-but-non-zero counts/ranges wouldn't alert) — possible future hardening. CONTEXT → **v0.4.4**.
- *(Audit-tooling note: `gh` reads its keyring only from the Bash shell in this dev env, not PowerShell — gh checks were run via Bash; the `revision:path` arg also needs PowerShell to avoid MSYS mangling. Neither affects CI.)*
- **Next (Architect):** with the pipeline + scheduler proven, the open product items — monthly roll-up, value-lens/fidelity, Drive mirror, optional live-output sanity guard.

### 2026-06-22 — CODE TASK #15: daily price-change email alert (Code)
- New module **`mobile_tracker/alerts.py`**: after each live run, a plan whose `price_brl` moves **≥3%** (up OR down) vs the previous snapshot → a **digest email** to <your-alert-recipient> from <your-sender-gmail>. Matched by **(carrier, state, plan_id)** — same identity as the `changes` sheet (they agree). New/removed plans are not price-move alerts.
- **Functions:** `compute_price_alerts` (pure), `format_alert_email` (pure: subject + one line/plan sorted by |%| desc, with ▲/▼), `send_alert_email` (STARTTLS `smtp.gmail.com:587`), `alerts_from_workbook` (reads the workbook's last two snapshots). Wired into `main.py` after `write_workbook`, **live only**.
- **Credential — secret ONLY:** the SMTP app-password is read solely from the **`EMAIL_APP_PASSWORD`** env var (a GitHub Actions Secret). **Never hardcoded/printed/committed** — verified no literal anywhere. The workflow passes `EMAIL_APP_PASSWORD: ${{ secrets.EMAIL_APP_PASSWORD }}` to the "Run tracker" step. Addresses + `threshold_pct: 3.0` live in `config/sources.yaml` (`alerts:` — non-secret; added `Settings.alerts`).
- **Graceful degradation (collection > notification):** missing/empty password → log "skipping" + skip; SMTP failure → caught + logged; the whole alert block in `main.py` is wrapped so it can **never fail the scrape or block the data commit**. Console log lines are ASCII-safe (email body keeps the ≥/▲/▼ Unicode).
- **First fire = NEXT run:** needs ≥2 snapshots, so the first alert compares the next daily run to today's.
- **Tests 52 → 60:** rise ▲ / drop ▼ / <3% ignored / exactly-3% flagged / new+removed not flagged / matched-by-plan_id-not-name; subject+body format; **send skips without password**; **SMTP error caught** (no raise); `alerts_from_workbook` needs 2 snapshots. No real SMTP hit. CONTEXT → **v0.4.5**.
- **HARD PRE-REQ for live alerts (Bridge):** add the `EMAIL_APP_PASSWORD` secret (Gmail app password for <your-sender-gmail>) in repo → Settings → Secrets and variables → Actions. Until then the step skips gracefully.
- No workbook/secret committed.
- **Next (Architect):** monthly roll-up; value-lens/fidelity; Drive mirror; the live-output sanity guard.

### 2026-06-26 — CODE TASK #16: comparison matrix DAILY → MONTHLY (Figure-5 form) (Code)
- **`comparison` is now the MONTHLY evolution matrix:** rows = **month** (one per distinct month in `history`, chronological), stored as a **first-of-month DATE** (`2026-06-01`) display-formatted **`mmm-yy`** (→ Jun-26); built in Python by deduping `snapshot_date` to year-month (`evolution_months`). Columns unchanged: `Month` + four groups (**Control / Post / Pre / Digital**) each merged over **TIM | Vivo | Claro**. Each value cell = a **live `_xlfn.MINIFS` over `history`** = **cheapest R$ that carrier offered in that category DURING the month** → workbook stays self-connected and grows **one row per month**; daily raw detail stays in `history`. Presentation/formula only — adapters/schema/scraping/`history` untouched.
- **Charts → monthly:** the four per-category line charts now plot over the **Month** X-axis; refs rebuilt each run to the months present; palette colors + markers unchanged. Per-group heatmap + #11 house style + TIM/Vivo/Claro order + same-day idempotency + the #15 alert all intact.
- **Key finding — EOMONTH date-range bounds are infeasible here (verified by recalc in REAL Excel, COM v16):** `history.snapshot_date` is stored as **TEXT**, and Excel **coerces** a date/serial criterion (incl. `">="&$A2` and `"<="&_xlfn.EOMONTH($A2,0)`) to a number that **never matches a text column** → every EOMONTH/`>=`/`<=` variant returned **0/blank** in a recalc probe. The **only** form that filters the text dates is a **MINIFS prefix wildcard**: match `snapshot_date` against `YEAR($A<row>)&"-"&TEXT(MONTH($A<row>),"00")&"-*"`. The prefix uses a **numeric `"00"`** (not `"yyyy"/"mm"`, which pt-BR Excel localizes — `TEXT($A1,"yyyy-mm")` here returns `"yyyy-06"`), so it's locale-independent. `_minifs_month` + CONTEXT §7 document this; **flagged for the Architect** (literal EOMONTH bounds would require storing `history` dates as real Excel dates — a `history`-data change this task excluded).
- **Tests 60 → 64** (`tests/test_evolution.py` rebuilt for monthly): one matrix row per month (first-of-month date, `mmm-yy` format), value cells are month-scoped MINIFS over `history` (text-prefix from the month cell, incl. the reciprocal-max Digital structure), four charts on the monthly column, heatmap CF present, `evolution_months` helper, **two snapshots in one month roll up to ONE row while daily detail stays in `history`**, **Dec→Jan year-boundary**, Ranking preserved, same-day idempotency. (Last two added from an adversarial self-review.) Existing 60 stay green. Offline, no network.
- **Review copy regenerated OFFLINE from committed `history`** (no scrape) and **recalculated in Excel to confirm it populates**: history has 1 date (2026-06-22) → matrix shows the single **Jun-26** row — Control(T/V/C) 58.99/59/59.9 · Post 119.99/150/124.9 · Pre 20/17/1 · Digital (TIM blank)/30/44.9. A synthetic May+June recalc confirmed two rows with correct per-month cheapest values (grows one row/month). Left at `data/mobile_plans.xlsx` **uncommitted** (the daily job owns the committed copy); workbook NOT committed. CONTEXT → **v0.4.6**.
- **Next (Architect):** event annotations from `changes` on the matrix; value-lens/fidelity refinements; Drive mirror; the live-output sanity guard. (As months accrue the matrix/charts fill in automatically.)

### 2026-06-26 — CODE TASK #17: split evolution view — table-only `comparison` + `Charts` sheet (Code)
- **Fixed the "locked" view.** Diagnosed: `comparison` opened showing only charts with no way to scroll to the table. **Root cause was NOT sheet protection** (it was never protected) — the 4 charts overlaid the top ~35 rows, the matrix started at row 35, and **`freeze_panes` pinned a 36-row block (`B37`)**, so the frozen chart region filled the viewport. Layout/presentation only — matrix formulas, data, schema, scraping untouched.
- **`comparison` is now TABLE-ONLY at the top:** group titles row 1 (each merged over its 3 carrier cols), **TIM | Vivo | Claro** sub-header row 2, month rows from **row 3**. Frozen at **`B3`** (just the 2 header rows + the Month column → scrolls freely). The live month-scoped `_xlfn.MINIFS` formulas (text-prefix form from #16, unchanged logic — just at the new row positions), the per-group heatmap, and the #11 house style all kept. Sheet is **not protected**.
- **New `Charts` sheet** (tab `#43A047`): the 4 per-category line charts (Control / Post / Pre / Digital) in a 2×2 layout, each referencing the `comparison` matrix via **cross-sheet refs** (X = `'comparison'!$A$3:$A$<lastmonth>`; 3 series = the group's TIM/Vivo/Claro matrix columns), palette colors + markers + monthly X-axis preserved, rebuilt each run to the months present. `_line_chart` now takes (chart_ws, data_ws); `_write_comparison` returns `(ws, last)`; new `_write_charts`.
- **`comparison` is the active sheet on open** (`wb.active` + only-selected-tab), so the workbook lands on the table, not `history`. Sheet count **9 → 10**.
- **Verified in real Excel (COM v16):** opens on `comparison`; **comparison ChartObjects=0, Charts ChartObjects=4**; `comparison.ProtectContents=False`; header A1=`Month`/B1=`Control (R$/mo)`/B2=`TIM`; row 3 = `jun/26` populates (Post 119.99/150/124.9, Digital (TIM blank)/30/44.9); 10 sheets in order history…**comparison, Charts**…TIM. Everything else (latest/changes/summary/Ranking/Vivo/Claro/TIM, idempotency, #15 alert) intact.
- **Tests 64 → 67** (`tests/test_evolution.py` updated for the new positions H1/H2/FIRST=1/2/3): `comparison` table at row 1–2 header / row 3+ data, **no `<drawing>` on `comparison` + a `<drawing>` on `Charts`** (charts moved), `Charts` sheet exists with 4 LineCharts referencing the matrix, `freeze_panes=="B3"`, `protection.sheet is False`, `wb.active=="comparison"`, formulas still month-scoped MINIFS over history, idempotency intact. Offline, no network.
- **Review copy** regenerated OFFLINE from committed `history` (4 June snapshots) and recalc-verified; left at `data/mobile_plans.xlsx` **uncommitted** (daily job owns the committed copy). Workbook NOT committed. CONTEXT → **v0.4.7**.
- **Next (Architect):** event annotations from `changes`; value-lens/fidelity; Drive mirror; live-output sanity guard.

### 2026-06-26 — CODE TASK #18: prepaid 30-day plan + validity_days (first ADAPTER change) (Code)
- **Schema:** added nullable **`validity_days`** (int) to the `Plan` model + `COLUMNS` (after `data_note`) — populated for prepaid, blank for monthly plans. CONTEXT §6 + §3 updated.
- **Claro R$1 → R$30 (the bug):** the old parser captured "Prezão R$1 por dia" (the per-day headline = R$30÷30). `parse_claro_prepaid` rewritten to regex the page's rich-text recarga tiers `"R$ <recarga>/<validity> dias – <gb>GB"` (dash-required so an adjacent GB can't bleed in), keyed `claro:prezao-<days>d-<gb>gb` (validity+gb, never price-derived); on a (validity,gb) collision keep the cheaper recarga. **Live: 7 tiers (12d→30d); the 30-day = R$30/12GB.**
- **Vivo / TIM validity:** Vivo reads each prepaid card's `"30 dias"` (gated on prepaid → postpaid/control/lite untouched); TIM has **no clean validity field** → `validity_days` = MAX "N dias" in the oferta's benefits HTML (plan period dominates bonuses). **Live: Vivo R$30/25GB/30d (+22/17/15d tiers); TIM all 3 XIP = 30d (R$20/6GB, R$25/8GB, R$30/16GB).**
- **Matrix Pre column:** now "Pre (R$/mo, 30-day)" = cheapest prepaid with `validity_days >= 28` (config `prepaid.min_validity_days: 28`; mirrored in `excel_writer.PREPAID_MIN_VALIDITY_DAYS`), a live MINIFS with one extra criterion `history!$M:$M,">=28"`. Operator sheets show validity (`· 30d`) in the Data cell. **Pre row recalculated in Excel = TIM 20 / Vivo 30 / Claro 30.**
- **Bug found+fixed (column drift):** merging a pre-#18 history (no `validity_days`) made `pd.concat` append the column at the END, so the matrix formula `history!$M:$M` pointed at the wrong column and Pre read **blank**. `_merge_history` now **reindexes to `COLUMNS`** (schema order always enforced). Caught by a recalc in Excel; added a `_merge_history` regression test.
- **Live re-scrape (`python -m mobile_tracker.main`, 2026-06-26):** `Collected 56 valid plans` (claro 20 / tim 15 / vivo 21), exit 0, no email (password unset locally). Non-prepaid counts unchanged for claro/tim; Vivo non-prepaid varies run-to-run (Playwright render drift) — proven NOT my change by re-parsing the saved 06-22 captures (identical output, validity=None).
- **Tests 67 → 73:** Claro 30-day+validity, Vivo prepaid validity + postpaid-no-validity, TIM prepaid validity (synthetic) + control-no-validity, matrix Pre validity-filter formula, `_merge_history` schema-order. Offline, no network. Existing 67 green.
- **WIP handling (Bridge YES):** the pre-existing uncommitted **"sanity guardrail" WIP** (base.py/alerts.py/config.py/main.py/sources.yaml/claro.py/vivo.py — NOT mine) was **stashed** for a clean #18, restored after. My #18 commit contains only #18.
- **Review copy** regenerated from the live snapshot, left at `data/mobile_plans.xlsx` (uncommitted). Workbook NOT committed. CONTEXT → **v0.5.0**.
- **Open question for the Bridge (REPORT BACK):** Pre display = monthly **R$/mo** (default, shipped) vs **avg R$/day** (JPMorgan-style = price ÷ validity)? A one-line switch.
- **Next (Architect):** confirm the Pre display; fidelity vs boleto (#19); the value-lens / R$-per-GB; the sanity-guardrail WIP (still uncommitted).

### 2026-06-26 — CODE TASK #19: comparison matrix MONTHLY → DAILY rows + green→yellow heatmap (Code)
- **Matrix is now DAILY** (presentation/formula only — adapters/schema/scraping/`history` untouched). Rows = one per distinct `snapshot_date` (`evolution_dates`), **earliest first** (2026-06-22 at the top), each a real DATE formatted `dd-mmm` (→ 22-Jun); header A1 = "Date". It **auto-grows one row per day** as the daily job adds snapshots.
- **Exact-date MINIFS:** `_minifs_day` matches `history`'s TEXT `snapshot_date` against a locale-safe exact date built from the row's date — `YEAR($A<row>)&"-"&TEXT(MONTH($A<row>),"00")&"-"&TEXT(DAY($A<row>),"00")` (= "2026-06-22", no wildcard). Same #16 text-date learning, exact-date instead of the month prefix. Category mapping (Control/Post/Pre/Digital), TIM|Vivo|Claro order, 2-row header, Pre = cheapest 30-day plan (validity>=28, #18), Digital = min(lite,flex) all preserved.
- **Heatmap → JPMorgan green→yellow, per carrier column:** replaced the 3-colour green→yellow→red group scale with a **2-colour green→yellow** scale (`_price_scale_2color`, soft `#A9D08E`→`#FFE699`, no red), applied to **each carrier column independently** (own min/max) so each carrier's evolution reads down its column. The 3-colour red scale stays on latest/operator/Ranking. In-sheet note: subtle while near-flat, differentiates as prices move.
- **Charts** (on the `Charts` sheet, #17) → **daily X-axis**; refs rebuilt to the dates present. Table-only layout, freeze B3, active sheet = comparison, idempotency, the #15 alert — all intact.
- **Prepaid-validity transition (expected, NOT a bug):** pre-#18 snapshots (Jun 22–25) have no `validity_days`, so their daily Pre cells are blank (filtered by `>=28`) and **populate from Jun-26 on**; old Claro R$1 rows drop out of Pre. The #18 schema change taking effect going forward; self-heals as days accrue.
- **Verified in real Excel (COM):** opens on `comparison`; A1="Date"; **5 daily rows** 22–26 Jun (earliest first); Post populates all days (119.99/150/124.9); **Pre blank 22–25 Jun, then 20/30/30 on 26-Jun**; comparison ChartObjects=0 / Charts=4. Review copy regenerated OFFLINE from the on-disk history, left at `data/mobile_plans.xlsx` (uncommitted).
- **Tests updated for daily** (`tests/test_evolution.py`): one row per date (earliest first, real date, `dd-mmm`), exact-date MINIFS (YEAR/MONTH/DAY, no wildcard), distinct-dates-one-row-each, **green→yellow 2-colour no-red** heatmap, charts on the daily matrix, table-only/active/frozen layout, idempotency. Suite green (**76**). No network.
- **WIP untouched:** #19 only changed `excel_writer.py` + tests + docs — no overlap with the sanity-guardrail WIP, so committed cleanly (no stash needed). CONTEXT → **v0.5.1**.
- **Next (Architect):** the fidelity-vs-boleto / value-lens refinements; the sanity-guardrail WIP (still uncommitted); event annotations from `changes`.

### 2026-06-26 — CODE TASK #20: match JPMorgan visuals — per-block heatmap + styled charts + bar chart (Code)
- **Presentation only** — no value/metric/adapter/schema/history change; the matrix stays entry-level (cheapest per carrier/category), formulas unchanged.
- **Heatmap → per-category-BLOCK** (was per-column in #19): replaced the 12 per-column CF ranges with **4 per-category-block** ranges — Control `B3:D<last>`, Post `E3:G<last>`, Pre `H3:J<last>`, Digital `K3:M<last>` — each a single 2-colour green→yellow scale (soft `#A9D08E`→`#FFE699`, no red) over its 3 carrier columns × all date rows, so a cell's colour reflects its price **vs all carriers in that category**. Rebuilt to the rows present each run.
- **Line charts styled** (Charts sheet): legend bottom, **vertical gridlines removed** (`x_axis.majorGridlines=None`), carrier-coloured lines + markers, Y="R$", concise titles. Palette kept (TIM `#0033A0` / Vivo `#660099` / Claro `#DA291C`).
- **NEW current-price grouped BAR chart** (JPMorgan Fig-11 style) on `Charts`: X = Control/Post/Pre/Digital, 3 bars each = TIM/Vivo/Claro, values = the **MOST RECENT day's** entry-level price. Structure-driven via a small **helper table at P37** holding **cross-sheet refs to the LAST matrix row** (`='comparison'!<col><last>`) — rebuilt each run so it **auto-updates** as new days land (no hardcoded numbers). Pre bar = the 30-day monthly value. Title "Current entry-level price by carrier (R$)", data labels on, carrier-palette bars. `_current_price_bar` in `excel_writer.py`.
- **Gridlines OFF re-verified on all 10 sheets** (openpyxl `showGridLines is False` for every sheet); charts free of heavy gridlines.
- **Verified in real Excel (COM):** opens on `comparison`; **comparison ChartObjects=0, Charts ChartObjects=5** (4 line + 1 bar, clustered-column type 51); the bar helper reads the **26-Jun** matrix row — Control 58.99/59/59.9, Post 119.99/150/124.9, **Pre 20/30/30**, Digital (TIM blank)/30/44.9. Review copy regenerated OFFLINE, left at `data/mobile_plans.xlsx` (uncommitted).
- **Tests** (`tests/test_evolution.py`): per-block heatmap (4 ranges B3:D5/E3:G5/H3:J5/K3:M5, green→yellow, no red), the new bar chart (helper refs the last row, 1 barChart + 4 lineChart parts), line charts retain their matrix refs (filtered to lineChart parts), gridlines via test_styling. Suite green (**78**). No network.
- **WIP untouched:** #20 only changed `excel_writer.py` + tests + docs — no overlap with the sanity-guardrail WIP, committed cleanly (no stash). CONTEXT → **v0.5.2**.
- **Next (Architect):** value calibrations / fidelity-vs-boleto; the sanity-guardrail WIP (still uncommitted); event annotations.

### 2026-06-30 — CODE TASK #21: bake cached values so the committed workbook displays in any viewer (Code)
- **Diagnosed (confirmed on origin/main's committed workbook):** openpyxl writes formula cells as `<f>` with **NO cached `<v>` value**, so the comparison matrix (108 cells) + summary (12 cells) return **None** under `data_only=True` → **BLANK in any non-recalc viewer** (GitHub preview, Google Sheets, Numbers, mobile, Excel Protected View). Desktop Excel recalcs on open (`fullCalcOnLoad=1`), which had masked it.
- **Fix — Python cached-value BAKE (keeps formulas):** new `_bake_cached_values(path, {...})` in `excel_writer.py`, called at the end of `write_workbook` after openpyxl saves. It injects a `<v>value</v>` **alongside** each matrix/summary `<f>` (never replacing the formula). Values computed in Python — `_matrix_value` (cheapest per carrier/category/exact-date; Pre validity≥28; digital min) and `_write_summary` (count/min/avg/max over `latest`) — with the **same logic as the formulas**. Surgical: a per-cell string insert that rewrites ONLY the `comparison`/`summary` sheet XMLs; every other zip part (charts, CF, drawings, tab colors) passes through **byte-identical**.
- **Chose the Python bake over LibreOffice recalc** (the Architect's first suggestion) — reported per the task: LibreOffice isn't installed locally (couldn't "test locally first") and its xlsx round-trip re-exports the 5 charts + CF + merged cells, risking the mangling the task flags as a STOP. The bake is fully testable locally, CI-native (no install), and can't touch charts. This is the task's sanctioned fallback ("compute the values in Python and bake them as cached values while keeping the formulas") — and openpyxl genuinely can't hold both via its API, so the bake does it via a targeted post-save XML injection.
- **NO workflow change needed:** the bake lives in `write_workbook`, which the daily job already runs (`python -m mobile_tracker.main`) — so every daily commit is now a values-baked workbook automatically. No LibreOffice apt-install, faster CI.
- **Verified:** regenerated from committed history (9 dates) → `data_only=True` shows **87/108** matrix values (the other 21 are correctly-blank no-offer cells: 12 Pre pre-#18 + 9 TIM-Digital) incl. **Control 22-Jun = 58.99/59/59.9**, Post 119.99/150/124.9, summary KPIs; `data_only=False` still shows the MINIFS formulas; **5 charts + 10 sheets intact**. Values match Excel's recalc (verified via COM on this history in #19/#20; local COM Open glitched this session, so relied on openpyxl `data_only` — which IS exactly what a non-recalc viewer reads).
- **Adversarial review caught a BLOCKER (fixed):** openpyxl **3.1.x** writes a formula cell as `<f>…</f>` + an **EMPTY value placeholder** — `<v/>` (lxml) or `<v></v>` (stdlib etree) — not bare `<f>`. My first injector (skip if `<v>` present) baked **0 cells** on the stdlib shape and made a **duplicate `<v>`** on the lxml shape (my local 3.1.5) — worked by luck locally, would ship blank in CI. Fixed: the injector now **drops the empty placeholder and writes exactly one `<v>`** (handles `<v/>`, `<v></v>`, no-`<v>`, real-value=idempotent). Added a **fail-loud guard**: `write_workbook` raises if baked-count ≠ expected, so a future openpyxl change can't silently ship a blank workbook. Also int-normalized cached values (59, not 59.0).
- **Tests 78 → 81**: `test_formula_cells_have_cached_values_baked` (formulas present `data_only=False` AND correct cached values `data_only=True`) + `test_inject_cached_values_handles_all_openpyxl_v_shapes` (version-independent unit test of all `<v>` shapes) + `test_bake_guard_raises_on_incomplete_bake` (a simulated 0-bake makes write_workbook raise). Existing chart/heatmap/layout tests run on **baked** output and pass (proving the bake preserves charts/CF). Re-verified on openpyxl 3.1.5: single `<v>` per baked cell, 21 no-offer cells correctly blank, Control 22-Jun 58.99/59/59.9. No network.
- **WIP untouched:** #21 only changed `excel_writer.py` + tests + docs — no overlap with the sanity-guardrail WIP, committed cleanly (no stash). Synced (fast-forwarded past 6 daily-snapshot commits) before working. CONTEXT → **v0.5.3**.
- **Next (Architect):** value calibrations / fidelity-vs-boleto; the sanity-guardrail WIP (still uncommitted); event annotations.

### 2026-07-01 — CODE TASK #22: source-of-truth audit (SP data vs live sites) (Code)
- **Read-only audit** — no adapter/schema/matrix change. One live SP fetch of all 3 carriers (claro 20 / tim 15 / vivo 21), raw captures saved, compared field-by-field to what we parse. Full note: **`docs/source_audit_2026-07.md`**.
- **Region — SP confirmed for all three** (task's critical check): **Claro** `__NEXT_DATA__` `SEGMENTATION_DEFAULT.uf="SP"` (name "São Paulo", ddd 11, cityChangedByUser=false; "Salvador" hits are only nav links); **Vivo** active-city label **"São Paulo (SP)"**; **TIM** `/sp/` URL path. No page defaulted to another city.
- **MATCH/MISMATCH table** (per carrier × category) in the note. **Confirmed CORRECT:** postpaid cheapest **TIM R$119,99 < Claro R$124,90 < Vivo R$150**; Claro Control **59,90 → 54,90** (fatura/conta digital, both captured); Claro Prezão **30-day = R$30/12GB**; Vivo control/postpaid/prepaid (Vivo prepaid validity 15/17/22/30 correct); TIM control-effective + postpaid.
- **MISMATCH #1 — TIM prepaid validity (data-correctness bug from #18):** #18's **MAX-"N dias"** heuristic was wrong — a shared marketing line (*"…recarga de R$30 válidos por 30 dias"*) appears in **every** oferta, so it inflated all three to 30d. **True: R$20/6GB → 17d, R$25/8GB → 22d, R$30/16GB → 30d.** So the daily-matrix **Pre column shows TIM R$20 (a 17-day plan)** instead of the real 30-day **R$30**. Fix: parse each oferta's own *"R$&lt;price&gt;, válido por &lt;N&gt; dias"* (matching its `field_preco_card_oferta`).
- **MISMATCH #2 — Vivo Lite loyalty:** Easy Lite has a subscription toggle — **"Plano anual" (12-mo loyalty) R$30** vs **"Plano mensal / Sem fidelidade" ≈R$45**. We capture the **annual (loyalty)** price; the no-commitment ≈R$45 is **toggle-gated + NOT captured** (needs a Playwright click). Only `lite` has the toggle (control/postpaid single-price). Fix: capture the no-commitment price or label the loyalty one.
- **Minor:** TIM control shows a list price (`field_preco_adicional_original` 63,99…) above the captured effective (58,99…, "Desconto Extra R$5"); we capture the effective only.
- **Fix list for the next task:** (1) TIM prepaid validity 17/22/30 [HIGH — corrects the Pre column]; (2) Vivo-Lite no-commitment price [MEDIUM]; (3) TIM control list price [LOW, optional].
- **WIP untouched:** #22 added only `docs/source_audit_2026-07.md` + CONTEXT/PROGRESS — no overlap with the sanity-guardrail WIP; committed cleanly. No workbook/raw committed. CONTEXT → **v0.5.4**.
- **Next (Architect):** fix TIM prepaid validity + the Vivo-Lite no-commitment capture (drive from the audit note); then value-lens / R$-per-GB; the sanity-guardrail WIP (still uncommitted).

### 2026-07-01 — CODE TASK #23: fix TIM prepaid validity + Vivo Lite no-commitment (adapter fixes) (Code)
- Resolves the two mismatches from #22's audit. **Adapter changes ONLY** (TIM prepaid validity + Vivo lite) — every other category/carrier untouched (both fixes are `category`-gated); matrix formulas + #21 cached-value bake unchanged.
- **FIX 1 — TIM prepaid validity → per-oferta** (`adapters/tim.py`): replaced #18's MAX-"N dias" heuristic (a shared *"recarga de R$30 válidos por 30 dias"* line appears in every oferta, inflating all to 30d) with `_prepaid_validity` — matches each oferta's own *"R$&lt;recarga&gt; … por &lt;N&gt; dias"* pair whose recarga equals that oferta's `field_preco_card_oferta`. **Live-verified (SP):** tim:59906 R$20/6GB→**17d**, tim:59901 R$25/8GB→**22d**, tim:106306 R$30/16GB→**30d**. → the daily-matrix **Pre column now reads TIM R$30** (the real 30-day plan), not the R$20/17-day plan.
- **FIX 2 — Vivo Lite no-commitment price** (`adapters/vivo.py`): the `lite` render now clicks each Easy Lite card's **"Sem fidelidade"** toggle (`_toggle_lite_no_commitment` in `_render`, gated on `category == "lite"`), so `.total-card-price-value` text = the **no-commitment monthly** (headline `price_brl`) while `data-original-price` keeps the **12-mo loyalty** price → `price_promo_brl` with `loyalty_months=12`. Single-price cards (text == original) get no promo. **Live-verified (SP):** R$45/30GB (loyalty 30), R$55/40GB (loyalty 40), R$35/20GB (single-price, no annual option).
- **Data nuance to flag:** the task assumed Vivo Digital ≈R$45, but the cheapest no-commitment Easy Lite is **R$35/20GB** (that card has no annual toggle) → the **Digital column = R$35**, not ~R$45.
- **Live re-scrape** (polite, SP only, 1×, real delays): **claro 20 / tim 15 / vivo 22**. Matrix Pre = TIM 30 / Vivo 30 / Claro 30; Digital = TIM –/ Vivo 35 / Claro 44,90.
- **Tests 81 → 82:** `test_tim.py::test_prepaid_validity_days_is_per_oferta_not_max` (fixture with the shared 30d line + own 17d → asserts 17, not 30); `test_vivo.py::test_lite_no_commitment_price_with_loyalty_promo` (data-original-price 30 / text 45 → price_brl 45, promo 30, loyalty_months 12; single-price card → no promo). Suite green.
- **WIP untouched:** committed only my 6 files (`adapters/tim.py`, `adapters/vivo.py`, `tests/test_tim.py`, `tests/test_vivo.py`, CONTEXT, PROGRESS); the sanity-guardrail WIP stays stashed/uncommitted. No workbook/raw committed. CONTEXT → **v0.5.5**.
- **Committed workbook regenerated** by a manual CI run (`gh workflow run mobile-price-tracker.yml`) after push — origin/main's `data/mobile_plans.xlsx` shows the corrected + baked values (TIM Pre R$30, Vivo Digital R$35) via `data_only=True`.
- **Next (Architect):** value-lens / R$-per-GB (§8 iv); TIM control list-price capture (§8 minor, LOW); the sanity-guardrail WIP (still uncommitted).

### 2026-07-01 — CODE TASK #24: TIM postpaid entry = bill-payment plan, not credit-card (Code)
- **JPMorgan rule:** TIM's cheapest postpaid (A Express 67GB **R$119,99**) requires a recurring **credit card** (low adoption), so the entry-level Post pick now uses the cheapest **bill-payment** plan = **TIM Black 70GB R$129,99**. Adapter/matrix change ONLY for the TIM postpaid entry-level pick; all plans stay in history.
- **How credit-card vs bill was identified (investigation, 1 live SP fetch):** the oferta's **`field_preco_principal_7_1_3`** HTML headline reads `R$ <price>/mês **no cartão**` (credit card) or `R$ <price>/mês **na fatura**` (bill). `field_formas_de_pagamento` is empty (`[]`) — useless. The two phrases are **mutually exclusive within that field** (verified: every fatura plan has 0 "cartão" tokens; every cartão plan has 0 "fatura") → reliable. The 3 "Express" plans = credit-card, the 4 non-Express = bill (name aligns, but we key off the explicit text). Live SP: credit_card = A Express 119,99 / B Express 144,99 / C Express 164,99; bill = 70GB **129,99** / Plus 149,99 / C Ultra 159,99 / Premium 159,99.
- **Implementation:** new **`payment_method`** field (`models.py` COLUMNS after `price_note` + Plan; reindex covers old rows). `tim.py` `_payment_method` tags postpaid only (gated on category). `excel_writer.py`: the Post `MINIFS` gets `history!<payment_method>,"<>credit_card"` (postpaid group only); the #21 bake `_matrix_value` mirrors it (drops `credit_card`, keeps blanks, case-insensitive) → **bake == live formula**.
- **Excel semantics VERIFIED (Excel COM recalc):** `MINIFS`/`COUNTIFS` with `"<>credit_card"` **INCLUDES blank cells** (empty ≠ credit_card; hist-all-blank → 119.99, COUNTIFS → 2, mixed → 135). So carriers without a credit-card tag (Vivo/Claro — blank) are **unaffected**, and pre-#24 history **self-heals** (blank → included → keeps old cheapest; freshly-scraped rows exclude the credit-card plan).
- **Live-data verify (no re-scrape):** re-parsed the saved live TIM postpaid capture with the new code → 7/7 tagged (3 credit_card, 4 bill, 0 untagged); `_matrix_value` TIM Post = **129.99** (R$119,99 credit-card excluded, all plans kept).
- **Adversarial review:** ran a 4-lens workflow (formula/bake agreement, scope containment, schema/history integrity, detection robustness) + verify pass over the diff — 0 blocking findings. One robustness note (dismissed as not-a-present-defect but harmless-to-fix): `_payment_method` scanned the whole HTML blob and checked "no cartão" first, so a hypothetical future bill plan with stray "no cartão" promo text could be misread as credit_card. **Hardened:** the classifier now anchors to the `/mês <phrase>` price headline (`_PAY_HEADLINE`), so only the price line decides — stray card/fatura marketing text can't flip it. +1 regression test.
- **Tests 82 → 89:** TIM postpaid tagging (credit_card vs bill), postpaid-only gating, `_payment_method` None fallback; matrix Post excludes credit-card + keeps blanks (Vivo unaffected), Post-only criterion (Control has none), `_matrix_value` unit (tagged → 129.99; blank historical → 119.99 self-heal). Suite green.
- **WIP untouched:** committed only my #24 files (`models.py`, `adapters/tim.py`, `excel_writer.py`, `tests/test_tim.py`, `tests/test_evolution.py`, CONTEXT, PROGRESS); the sanity-guardrail WIP stays uncommitted. No workbook/raw committed. CONTEXT → **v0.5.6**.
- **Committed workbook regenerated** via manual CI run (`gh workflow run mobile-price-tracker.yml`) after push; origin/main `data/mobile_plans.xlsx` shows the latest matrix **Post TIM = R$129,99** baked (`data_only=True`), historical rows self-heal (keep R$119,99 until re-scraped).
- **Next (Architect):** value-lens / R$-per-GB (§8 iv); TIM control list-price capture (§8 minor, LOW); the sanity-guardrail WIP (still uncommitted).

### 2026-07-02 — CODE TASK #25: TIM note + JPMorgan-style chart refinement (presentation) (Code)
- **Presentation only** — NO value/metric/adapter/schema/history/formula/bake change (`excel_writer.py` + tests only). openpyxl only.
- **TIM bill-payment note** added to the `comparison` sheet (beside the existing footnote): *"Post — TIM: the entry-level price is the cheapest BILL-payment plan (R$129,99), not the R$119,99 credit-card-only plan — credit-card-only plans have low adoption, so they are excluded from the entry-level comparison (JPMorgan methodology). All TIM plans still appear in `history` + the TIM sheet."*
- **Line charts → JPMorgan Figs 7/8/9** (`_line_chart`, still cross-sheet refs into the matrix, rebuilt each run): title **"<Category> – Monthly Prices"**, bottom legend, carrier palette (TIM `#0033A0` / Vivo `#660099` / Claro `#DA291C`), markers on, **one faint horizontal gridline** (no vertical, shared `_clean_gridlines`), and a **FIXED Y range per category** (`EVOLUTION_YRANGE` — Control 40–70, Post 90–170, Pre 20–40, Digital 25–65) so the view stays stable as history grows. Pre uses the 30-day **MONTHLY** value (~R$20–40), not R$/day.
- **Bar chart → JPMorgan Fig 11** (`_current_price_bar`): X = Control/Post/Pre/Digital, 3 bars = TIM/Vivo/Claro = the LATEST matrix row (structure-driven helper table, auto-updates), data labels on, carrier bars, clean gridlines. Title "Current entry-level price by carrier (R$)".
- **Gridlines off on all sheets** (unchanged `_gridless`); charts free of heavy gridlines. **Out of scope:** JPMorgan vs-2020 / yearly-change charts (Figs 10, 12–20) need a 2020 baseline + years of history we don't have.
- **Review copy** regenerated **offline** (re-aggregated the committed history through `write_workbook` → new charts + note, same data: 529 plans / 10 snapshots / Post TIM still R$129,99 baked), verified, then `git checkout` — `data/mobile_plans.xlsx` left uncommitted.
- **Tests 89 → 92:** line-chart title + fixed Y-range + faint/one-gridline + palette + bottom legend; bar-chart clean gridlines + title; TIM note cell present (has 129,99 / 119,99 / JPMorgan). Existing suite green. CONTEXT → **v0.5.7** (§3 surfaced-in-sheet note; §7 chart refinement + out-of-scope).
- **Next (Architect):** value-lens / R$-per-GB (§8 iv); TIM control list-price capture (§8 minor, LOW); the sanity-guardrail WIP (still uncommitted).

### 2026-07-02 — CODE TASK #26: Vivo Lite entry = cheapest "Sem fidelidade" plan (R$45, not R$35) (Code)
- **Bridge-directed** (from a live-page review): the Digital column's Vivo/lite entry-level should be the cheapest plan that OFFERS a **"Sem fidelidade"** choice, not the absolute-cheapest monthly tier. `excel_writer` only — no adapter/schema/history/bake-mechanism change.
- **Live investigation (1 SP render):** the Easy Lite page serves **3** `.unique-card`s — **20GB "Plano mensal" R$35** (monthly only, no toggle), **30GB R$45** (Sem fidelidade; anual R$30), **40GB R$55** (Sem fidelidade; anual R$40). The R$30/R$40 are the **annual/loyalty** prices (already captured as `price_promo_brl`, `loyalty_months=12` since #23). The screenshot's 4th "OFERTA POR TEMPO LIMITADO" 40GB R$40 card (YouTube Premium) is **not in the headless render** (transient/personalized promo); its R$40 annual price is already the 40GB row's loyalty promo. So there was **no collapse and nothing missing** — I corrected my earlier "collapse" guess.
- **Change:** the Digital **lite** `MINIFS` now requires `history!<loyalty_months>,">0"` and `_matrix_value` mirrors it — only toggle cards (loyalty set) count → **Digital Vivo = R$45** (the 20GB R$35 monthly-only tier is excluded from the *entry-level pick* but stays in history/the Vivo sheet). **Flex (Claro) unrestricted** (no toggle) → Digital Claro still R$44,90. Same spirit as the TIM credit-card exclusion (#24).
- **Verified formula == bake:** offline re-aggregation of the committed history → baked Digital Vivo 2026-07-01 = **45**, June 22–30 = blank; **Excel COM recalc** of the live formula gives **R$45.00** (07-01) and blank (06-22), Post TIM R$129,99 + Digital Claro R$44,90 unchanged. `">0"` on loyalty_months excludes blanks (Excel numeric), so pre-#23 lite rows **self-heal to blank** (like #18/#23).
- **Tests 92 → 94:** Digital lite gated on loyalty (picks R$45 not R$35, formula carries the criterion, Claro flex unchanged, all 3 tiers kept in history) + `_matrix_value` unit (loyalty-gated + blank self-heal). Suite green.
- **Committed workbook regenerated** via manual CI run after push; origin/main Digital Vivo latest = **R$45** baked. Review copy regenerated offline + `git checkout`. WIP (7 files) still uncommitted; no workbook/raw committed. CONTEXT → **v0.5.8** (§3 Digital nuance refined; §7 loyalty-gated lite).
- **Next (Architect):** value-lens / R$-per-GB (§8 iv); TIM control list-price capture (§8 minor, LOW); optionally harden the Vivo "Sem fidelidade" toggle to fail-loud; the sanity-guardrail WIP (still uncommitted).

### 2026-07-03 — CODE TASK #27: Post column back to the plain cheapest — TIM R$119,99 tracked (Bridge reversal of #24's pick) (Code)
- **Bridge's call:** the comparison table (and the committed GitHub workbook) should show **TIM's cheapest postpaid plan — R$119,99** (A Express, credit-card "no cartão") — and *change-detection* is the point: when that price moves, the `changes` sheet + the #15 daily ≥3% alert (both keyed on `plan_id` tim:55121) will flag it.
- **Change (`excel_writer` only):** removed the `"<>credit_card"` criterion from the Post `MINIFS` and its mirror filter in `_matrix_value` (the #21 bake). **Everything else from #24 retained:** the `payment_method` schema field, the tim.py "no cartão"/"na fatura" tagging, and the history data — billing type stays visible per plan; only the entry-level pick stopped excluding it.
- **Footnote rewritten (comparison sheet):** entry-level = the cheapest plan **R$119,99, flagged CREDIT-CARD-only**, with the cheapest bill plan (R$129,99) noted as context and a pointer to `payment_method`/`changes`.
- **Verified formula == bake:** offline re-aggregation of the committed history (585 plans / 11 snapshots) → baked **Post TIM = 119.99 on every date row** (incl. 07-01/07-02, which were 129.99 under #24 — the matrix recomputes from history each run, so the whole column self-corrects on regeneration); live formula carries **no** payment criterion; **Excel COM recalc** returns R$119.99. Digital Vivo stays R$45 (#26 untouched); Control/Pre/Claro/Vivo unchanged.
- **Tests 94 → 93:** the three #24 exclusion tests became two cheapest-pick tests (Post = 119.99 incl. credit_card + no criterion in the formula + both plans and tags kept in history; `_matrix_value` plain-min unit); the footnote test rewritten for the new wording. Suite green.
- **Committed workbook regenerated** via manual CI run after push — origin/main shows **Post TIM = R$119,99 baked** (`data_only=True`). Review copy regenerated offline + `git checkout`. WIP still uncommitted; no workbook/raw committed. CONTEXT → **v0.5.9**.
- **Next (Architect):** value-lens / R$-per-GB (§8 iv); TIM control list-price capture (§8 minor, LOW); optionally harden the Vivo "Sem fidelidade" toggle to fail-loud; the sanity-guardrail WIP (still uncommitted).

### 2026-07-13 — CODE TASKs #28 + #29: TIM Controle Fit added + the missed-alert root cause fixed (Code)
- **#28 — TIM Controle Fit (Bridge request: "TIM launched the Claro-Flex/Vivo-Lite peer, called TIM Fit"):** live recon found it as **"TIM Controle Fit"** — NOT a standalone page and **NOT in the drupal-settings `ofertas` JSON**: it's a server-rendered TEXT section ON the controle page (anchor `#price-fit`) with two versions in tabs. **Anual = 30GB, 12x R$30/mês no cartão de crédito, COM prazo de permanência (12m)**; **Mensal = 20GB, R$35/mês, SEM prazo** (SP, 2026-07-13). Each version's benefits modal carries a **stable etiqueta offer code** → plan_ids `tim:TIM202600000271` (anual) / `tim:TIM202600000270` (mensal) — carrier-native, rotation-proof.
- **Implementation:** new category **`fit`** (`VALID_CATEGORIES`, §6) + a `fit` target in `sources.yaml` on the SAME controle URL (one extra polite fetch/day; raw → `tim_fit_SP.html`); `parse_tim_fit_html` in tim.py (anchored regex over the `#price-fit` slice; section absent → `[]` + a loud fetch WARNING — see the review fixes below). **Digital column** (matrix + Ranking + operator sheets) now spans `{lite, flex, fit}`; the Digital MINIFS gains a fit term gated on **`loyalty_months` BLANK** (criterion `"="`) — the Anual's own price requires loyalty so only the no-commitment **Mensal sets the entry → Digital TIM = R$35** (the #26 rule applied to Fit's separate-plans shape). `_matrix_value` mirrors it (isna) so **bake == formula** — verified by an actual **Excel COM recalc** (Digital TIM K = R$35.00 live).
- **#29 — id-rotation-resilient change detection (the missed TIM Controle e-mail, root-caused):** on **2026-07-06** TIM republished EVERY Controle plan under a **fresh Drupal nid** while cutting prices (41GB 58,99→49,99 **−15.3%**, Premium 84,99→69,99 **−17.6%**, Plus 64,99→59,99 −7.7%). Id-only matching read that as 5 `removed` + 5 `new` → **no `price_change`, no alert** — the alert logic worked; the *identity* broke. Fix in BOTH `_compute_changes` and `compute_price_alerts`: id-unmatched rows re-pair by **(carrier, state, category, plan_name)** — **1:1 unambiguous only** (ambiguity → honest new/removed, never a guessed alert); a re-paired price move → `price_change` under the NEW id + the e-mail fires; a same-price re-key stays silent. Id-first matching unchanged; history untouched (append-only).
- **Tests 93 → 105:** fit parser (both versions, codes as ids, no-section → [], control-path isolation), Digital fit gating (workbook + `_matrix_value` unit), changes-sheet rotation (price_change under the new id, pure re-key silent, ambiguity stays new/removed), alerts rotation (the real 07-06 scenario → 3 alerts sorted by |pct|, ambiguity guards both sides, id-first preserved), + the 3 review-fix tests below. One old test REWRITTEN by design: `test_matched_by_plan_id_not_name` asserted the very behavior that swallowed the 07-06 alert.
- **Adversarial review (multi-agent, 3 lenses + per-finding verification) caught 4 real defects — all fixed pre-commit:** (1) MAJOR `_fit_price` stripped dots while the regex accepts dot-decimals ("35.90" → 3590.0: silent history corruption + a false alert next day) → dot kept as decimal separator; (2) MAJOR `_fit_code`'s 80k window wasn't bounded to its modal — if TIM drops one etiqueta link, the search would steal the SIBLING modal's code, colliding both plan_ids and silently deleting a row via `_merge_history` dedup → slice bounded at the next `id="modal-` + a seen-id guard forcing the per-version fallback; (3) the parser docstring claimed a per-target count safety net that doesn't exist (main's zero-guard is per-CARRIER; TIM's other categories would mask a vanished fit section) → honest docstring + a LOUD `WARNING tim/fit` print in fetch on an empty fit parse; (4) the Ranking sheet's Digital group would rank the Anual's 12-mo-commitment R$30 as rank-1 "cheapest" against competitors' no-commitment prices, contradicting its own caption → `_ranked` drops loyalty-gated fit versions from the Digital ranking only (they stay in history + the TIM sheet).
- Suite green (105). Excel COM verified the new `"="` criterion + rotation changes-sheet on a synthetic 2-day workbook; live-capture re-parse confirmed the bounded `_fit_code` still resolves both real etiqueta codes. Committed workbook regenerated via manual CI run after push (fit rows + Digital TIM baked — see REPORT). WIP restored after commit. CONTEXT → **v0.6.0** (§3 two new recon subsections, §6 `fit`, §7 Digital/changes updates).
- **Next (Architect):** watch the first daily runs' fit rows (Anual/Mensal stable?); value-lens / R$-per-GB (§8 iv); TIM control list-price capture (LOW); Vivo toggle fail-loud hardening; the sanity-guardrail WIP (still uncommitted).

### 2026-07-13 — CODE TASK #30: standing rule codified — NO loyalty headline prices, ever (Bridge, verbatim) (Code)
- **Bridge's standing rule (restated after the 07-12 lite incident):** the tracked headline/entry price is ALWAYS the **monthly price WITHOUT loyalty/fidelity** — every carrier, every category, **including TIM Fit**. Loyalty prices only as context (`price_promo_brl` + `loyalty_months`). Codified as **Bridge decision §10.5**; already enforced in all picks (#23/#26/#28 — Vivo lite Sem-fidelidade headline, TIM Fit Mensal entry, loyalty versions excluded from Digital matrix + Ranking).
- **Gap closed (the one place loyalty prices could still slip in silently):** when the Vivo "Sem fidelidade" toggle click fails, the lite headlines captured are the LOYALTY defaults — exactly what happened on the **2026-07-12** snapshot (Easy Lite 30/40 loyalty instead of 45/55; showed up as spurious ±R$15 `price_change`s on 07-13). New pure detector **`lite_capture_lost_toggle`** (lite parse where NO plan carries `loyalty_months`) + `VivoAdapter.fetch` now **retries the render ONCE** and prints a **loud `WARNING vivo/lite`** if the capture still lacks the toggle — data still recorded (collection > notification), but never silently.
- **Tests 105 → 106** (detector unit: healthy / lost / empty). vivo.py-only change via the stash dance (WIP preserved). CONTEXT → **v0.6.1** (§10.5 + changelog). Rule also saved to Code's persistent memory so future sessions apply it by default.
- **Next (Architect):** watch the first daily runs' fit rows + any `WARNING vivo/lite` in CI logs; value-lens / R$-per-GB (§8 iv); TIM control list-price capture (LOW); the sanity-guardrail WIP (still uncommitted).

### 2026-07-17 — CODE TASK #25: 3-year promo-vs-entry study 2023–2026 (Code)
- ⚠️ **Numbering note for the Bridge:** this task arrived labeled **#25**, but the repo already used #25 for the 0.5.7 in-sheet-footnote task (and is past #30). Kept the Bridge's label as instructed — flagging the collision.
- **Coverage map:** **controle + pós complete for all 12 carrier-years.** TIM 2023–25 from Wayback carrier pages (controle 2023-03-01 / 2024-03-24 / 2025-09-05; pós 2023-07-26 / 2024-07-05; the 2025-pós hole filled from an archived melhorplano.net SP page — no carrier snapshot exists); Claro 2023–25 from Wayback `__NEXT_DATA__`/card_360 grids cross-corroborated by the Tecnoblog Jan-2024 "On"-portfolio tables (a stale static card block on the archived pages was identified and discarded); **Vivo entirely from press + archived aggregator** (Wayback serves only AEM/JS 403 shells — recon confirmed): Tecnoblog Feb-2023 + Mar-2024 full price tables, melhorplano.net Apr–May-2025. 2026 = our tracker's audited 2026-07-01 anchors. **Gaps (blank + noted):** pré 30-day 2023–25 (per-oferta validity parsing on dead pages would risk repeating the #18 max-"N dias" bug); TIM digital 2023–25 (structural — Fit launched Jul-2026).
- **Annual ratio table (flagship promo ÷ same-category entry, SP):** TIM 101.9 → 103.8 → 71.2 → **69.2%**; Vivo 100.0 → 63.6 → 71.4 → **66.7%**; Claro 300.4 → 120.0 → 83.3 → **236.4%** (Claro's flagships alternate flash offers and controle-based combos; anchor_category visible per row). Entry pós rose +23–25% over the window (TIM 104.99→129.99, Vivo 122→150, Claro 99.90→124.90) while flagship promo prices fell.
- **2026 convergence face-off:** TIM **Ultracombo** entry R$89.99 (65GB+500M, déb. automático, partly SP-exclusive) = **69%** of entry pós; Vivo **Total Ultra R$100** (700M+70GB, May–Jun, closed — live site now Essencial R$130 "de 160") = **67%**; **Claro Multi** cheapest pós-based combo R$159.90 = **128%** (its limited-time entry combo 350M+Controle 41GB+Globoplay R$129.80 is controle-anchored → 236%). ⚠️ The Bridge-reported "new mid-July Vivo convergent launch" was **not found** in any reputable source — reported as such, no guess substituted (verifiable mid-July Vivo moves: the live limited-time Total repricing + the Jul-14 fiber-only 1-Giga cut to R$200).
- **Findings:** (1) promos drifted decisively **down-market** — from bonus-GB-at-list (≈100–104%, 2023–24) to real cuts (59–84%, 2025–26); (2) the **July-2026 combo wave is a break**: TIM+Vivo price convergent bundles BELOW their own entry pós for the first time in the series, Claro keeps "more-for-more" (its 2023 Multi Week was 300%); (3) list up, promo down — the competition moved into the spread; (4) digital is the quiet riser (Claro Flex +50% 2023→26); (5) TIM's 2026 pivot is the sharpest single-carrier shift: −15% controle repricing (Jul-06, our #29 catch) + TIM Play (Jul-07) + Ultracombo (Jul-16) in ten days.
- **Verified:** `check.py` green (standalone + pytest 4/4) — output: "(a) ratios recomputed OK: 29 rows (12 annual + 17 events); (b) sanity bands OK (controle 35-80, pos 80-200, ratio 40-400; blanks carry gap notes); (c) source-per-point OK: 65 non-blank data rows, all source_ids resolve (37 sources); (d) 2026 anchors == tracker audited values (incl. Claro 54.90 eff / 59.90 reg; TIM pos 129.99 bill); ALL GREEN". 2026 cross-check against `data/mobile_plans.xlsx` history passed on the 2026-07-01 snapshot (all six anchors + TIM Black 70GB bill-payment confirmed).
- **Files:** `analysis/promo_vs_entry/{promo_vs_entry.xlsx (annual/events/entry_prices/sources/Chart — house palette, gridlines off), report.md, sources.md, check.py}`. **No pipeline files touched** (pre-existing uncommitted WIP in adapters/config left exactly as found).
- **CONTEXT → v0.7.0** (new §12 + changelog; the task template said "bump to 0.6.0" but 0.6.x was already taken by #28–#30 — minor-bumped to 0.7.0 as intended).
- **Next (Architect):** extend the series natively from the tracker each year (§12 closing note); promotions-view backlog (§7) is the natural home for keeping the events log current; consider a pré 30-day backfill only if a live-verifiable method appears.

### 2026-07-17 — CODE TASK #26: convergent-offers English snapshot — TIM / Vivo / Claro (Code)
- **Scraped fresh (SP, polite one pass):** **Vivo Total 11 combos** (Playwright render; SP verified via the "São Paulo (SP)" label; grid = the same `.unique-card` component as the daily adapter) and **Claro Multi 5 combos** (Playwright over the page's `tab_select` "Fibra + Móvel" grid; SP segmentation confirmed in the page JSON; **the full grid IS public** — no CEP/address gate for the listing, no checkout entered). **TIM Ultracombo 7 rows** from TIM's published table, verified against the Teletime 07-16 article (range 89.99–314.99, 65–200GB, 500M/600M/1G, Globoplay/Paramount+, and the R$ 314.99 Família-VIP example all match); TIM's own site has **no Ultracombo page yet** (3 URL probes → 404).
- **Entry-price face-off (as verified):** **TIM R$ 89.99\*** (65GB + 500 Mbps; SP-only, débito automático) vs **Claro R$ 129.80** (Controle 41GB + 350 Mbps + Globoplay, limited-time) vs **Vivo R$ 160** (Total Pro 60GB + 500 Mbps).
- **`*` footnote confirmed** (TIM's table caption via Teletime, not guessed): *"Ofertas disponíveis apenas em São Paulo"*; separately, ALL TIM prices shown are débito-automático prices. Both translated on the Notes sheet.
- **Recon-lead divergences (flagged):** (a) Claro's real entry is **R$ 129.80** — the lead's R$ 159.90 combo exists but is the "Melhor escolha" mid-tier, and the full grid (5 combos to R$ 389.90) is public, not gated; (b) Vivo has **11** combos, not ~7 (4 are TV-bundle variants); (c) the suspicious **R$ 1,200** Vivo lead is **REAL** (V 1 Giga: 1 Gbps + 600 GB + 10 additional lines + Vivo TV Completo w/ HBO Max+Telecine); (d) Vivo serves **session-dependent promo variants** — #25's same-day capture saw "Essencial R$ 130 (de 160)"/"Ultra R$ 170 (de 190)" where this render got Pro R$ 160/Ultra R$ 170 flat (recorded in Notes); (e) a 6th Claro combo (600 Mbps + Pós 50GB) exists in the page JSON but is NOT rendered for SP — excluded.
- **Verified before commit:** workbook re-opened — 4 sheets (TIM/Vivo/Claro/Notes & Sources), row counts == combos parsed (7/11/5), **every price cell numeric** (TIM's `*` via number format `"R$" 0.00"*"`), house style + carrier tab colors + gridlines off; ≥2 rows per carrier spot-checked against the raw captures; all checks green.
- **Files:** `analysis/convergent_offers/{convergent_offers_2026-07.xlsx, build_workbook.py}`; raw HTML/JSON/screenshots under `analysis/convergent_offers/raw/` (**local only, not committed**). Daily pipeline untouched; these URLs NOT added to the daily config (one-shot per the task). CONTEXT → **v0.7.1** (new **§13** with the Vivo Total + Claro Multi parse paths — reusable adapter IP).
- **Next (Architect):** if the Bridge wants this refreshed on a cadence, §13's parse paths make a convergent-offers adapter a small task; otherwise nothing pending — snapshot is self-contained.

### 2026-08-03 — CODE TASK #31: convergent offers become a TRACKED domain — phase 1 (Code)
- **Numbering:** the Bridge called this "#26", but this repo has already used #26 twice (0.5.8 Vivo-Lite metric; 0.7.1 convergent snapshot), so it ships as **#31** per the task's own "use the NEXT number if #26 is taken". CONTEXT → **v0.8.0**, new **§14**.
- **STEP 0 verified:** correct project (Mobile Price Tracker — Vivo/Claro/TIM SP; `data/mobile_plans.xlsx` + `src/mobile_tracker/adapters/` present). No pitchbook/PPTX/DCF content — nothing telecom was written anywhere near `pe-pitch-maker`.
- **INVESTIGATION — all 3 SP combo pages, live (3 parallel recon agents, one polite pass each). ALL THREE ARE DISCRETE PRICED TIERS — none is a "monte seu combo" configurator:**
  - **TIM Ultracombo** `/sp/internet/tim-ultracombo` → **httpx 200, no browser** (Drupal `drupal-settings-json` → `ofertas`). **3 tiers.** ⚠️ **§13 was stale: the page 404'd on 07-17 and now EXISTS** (so #26's Teletime-sourced table is superseded); the tier set also went **7 → 3** in three weeks. SP proved 4 ways (`selectedState=sp`, `/sp/` base_url, view_args `…/sp`, `field_regioes` + `[SP]` title).
  - **Vivo Total** → httpx **403 (Cloudflare** now, not Akamai) → **Playwright required**; grid = the same `.unique-card` component as the mobile pages. **11 combos** (R$160 Pro → R$1,200 V 1 Giga; 5 carry TV). SP via the "São Paulo (SP)" label. Gotchas: the `VIV…` code is **NOT unique** (2 collisions across 11 cards), lazy render needs scrolling, and an optional paid apps add-on can hide the price element.
  - **Claro Multi** → **httpx now suffices** (overturning §13): `__NEXT_DATA__` `tab_select` for the grid + **one public** `GET /api/catalog?state=SP&city=sao_paulo&uuids=…` for prices (integer centavos; `precoCombo` when present else `preco`, summed per slot). **10 live combos + 5 "ghosts"** that the JSON lists but SP doesn't render (`catalog.notFound`). Stable id = the Storyblok `_uid`.
- **SCHEMA — a SEPARATE model** (`convergent.py`): `ConvergentOffer` / `CONVERGENT_COLUMNS`. Combos are structurally unlike `Plan` (several services, fiber speed, line count, TV tier), so forcing them into the mobile schema would have polluted it. `offer_id` carrier-native & never price-derived; derived `services` summary from four boolean flags; `is_valid()` requires **≥2 bundled services**; added **`payment_method`** because TIM prices combos on a real 3-price billing ladder.
- **`convergent_history` sheet** — append-only, one row per offer per snapshot_date, **same per-date idempotency as the mobile history** (a re-run replaces that date's rows). Deliberately flat: no formulas, no #21 bake, no charts, so it cannot perturb the mobile matrix. **The real hazard, found and handled:** `write_workbook` rebuilds the whole file (`ExcelWriter` mode `w`), so **any sheet not re-emitted is destroyed** — the convergent sheet is therefore **always read + re-written**, even on a run that collected nothing (a failed convergent scrape must never wipe convergent history). Locked down by a dedicated test.
- **FIRST SCRAPER — TIM Ultracombo** (`adapters/tim_convergent.py`, plain httpx; chosen as the cleanest non-Playwright source). **Live-verified SP:** `tim:167761` **R$ 89,99** (500 Mbps + 65 GB), `tim:167751` **R$ 139,99** (1 Gbps + 70 GB, Paramount+/Deezer), `tim:167756` **R$ 169,99** (1 Gbps + 115 GB, Paramount+/Deezer) — each fibre + **1** mobile line, no TV/landline; ids = native Drupal nids. Built around three verified traps: **(a) ghost price fields** — `field_preco_adicional_original`/`_tracejado` are identical on all three tiers and contradict the real prices, and the **mobile** TIM adapter reads that very field as a genuine struck-through price, so reusing it would have invented a fake "de R$149,99" promo on every combo (regression test added); **(b) `langcode` says "rj"** on SP-only offers → region gating uses `field_regioes`; **(c) streaming lives in icon-media names** (`"Icone Paramount - TIM Ultracombo"`), with an empty list being a real absence.
- **GUARDED WIRING (two layers).** `config/sources.yaml` gains a **`convergent:`** section (`active:` + `adapter:` per carrier; TIM active, Vivo/Claro staged for phase 2) and `Settings.convergent_targets()`; `main.run()` scrapes convergent **after** the mobile pass with per-target `try/except`, **plus** `try/except` around the merge and sheet-write **inside** `write_workbook` — so a convergent bug degrades to "keep what we had" rather than failing the mobile write. `--demo` skips the convergent pass entirely. Verified by tests: a convergent object that raises still yields a complete, correctly baked mobile workbook.
- **Mobile untouched:** no change to mobile adapters, schema, matrix, formulas, the #21 bake, charts, alerts or idempotency (only additive edits to `excel_writer`/`main`/`config`). Live end-to-end run wrote the 3 combos while preserving all **2,165 mobile rows / 39 snapshots**.
- **Tests 106 → 119** (+13): parser vs a trimmed real capture, ghost-price regression, region gate, payment ladder, streaming absence, empty/broken settings, the ≥2-services rule, `services` round-trip, sheet append + same-date idempotency, **preserve-on-empty**, the write-path guard, and demo-skips-convergent. Suite green.
- **Next (Architect):** phase 2 = the Vivo Total (Playwright, watch the non-unique `VIV` code) and Claro Multi (httpx + catalog API, filter `catalog.notFound` ghosts) adapters, then the convergent comparison sheet. Both parse paths are fully documented in §14/§13.

### 2026-08-03 — CODE TASK #32: convergent phase 2 — Vivo Total + Claro Multi adapters (Code)
- Completes the convergent domain started in #31: **all three carriers now scrape combo offers into `convergent_history`** on the guarded daily pass. Additive only — mobile adapters/schema/matrix/formulas/#21 bake/charts/alerts/idempotency untouched. CONTEXT → **v0.8.1** (§14 extended).
- **VIVO TOTAL** (`adapters/vivo_convergent.py`, Playwright — httpx is **403 behind Cloudflare**). **11 SP combos live-verified, R$160 → R$1 200**, 5 with TV: Pro 160 (500M/60GB) · Ultra 170 (700M/70GB) · Ultra+TV online 190 · Família 2/3/4/5 = 270/330/420/520 (120→300GB, 2→5 linhas) · Ultra+TV 290 · Família 2 TV-variants 290/390 · **V 1 Giga 1 200** (1 Gbps + 600GB + **11 linhas** + TV Completo c/ HBO Max e Telecine — real, not filtered as an outlier). SP asserted from the page's own "São Paulo (SP)" bar.
  - ⚠️ **`VIV` code collision handled:** 11 cards carry only 9 distinct codes (Ultra R$170 / Ultra+TV online R$190 both `VIV202604028853`; Total Família 2 R$270 / Família 2 R$290 both `VIV202604028085`). **`offer_id` = the VIV code + the card's own `productsIds`, sorted** (`vivo:VIV202604028853-305-313-408-…` vs `…-305-313-336-408-…`, token 336 = the TV product) — carrier-native, **never price-derived**, unique across all 11; a slug-then-position fallback guarantees no card is ever silently dropped. Read from `data-external-link-url` (some visible CTA hrefs are stale/copy-pasted).
  - ⚠️ **Lazy render** → bounded scroll until the card count stops growing (a no-scroll capture silently returns a SHORT list). ⚠️ **Hidden price** → the optional "6 apps por R$55/mês" toggle can `v-show` the price element away or show base+add-on; we never click it, fall back to `data-original-price`, and record the BASE price with a note (the add-on is never in the price, and the offer is never dropped). The visible `.unique-card__price` holds an unrendered Vue mustache, so the price only ever comes from `.total-card-price-value`.
  - **Session variants:** the code treats a struck-through `.unique-card__price-old` as the headline with the displayed price as the promo — but **no variant was served this run**: all 11 cards were flat (`price-old` empty, `data-original-price` == displayed), matching the 07-17 capture. The "Essencial R$130 (de 160)" variant §14 recorded did **not** appear. The card count is never hard-coded.
- **CLARO MULTI** (`adapters/claro_convergent.py`, **no browser** — two plain public GETs). **10 live SP combos, R$129,80 → R$459,80** across Fibra+Móvel / Fibra+TV / Fibra+Móvel+TV; entry = 350 Mbps + Controle 40GB.
  - Grid from `__NEXT_DATA__` → the component whose `component == "tab_select"` (**by name, never index** — a `card_360` of a different product line sits on the same page). `offer_id` = the Storyblok **`_uid`**.
  - Prices from **ONE batched** `GET /api/catalog?state=SP&city=sao_paulo&…&uuids=<all 15>`; **price = Σ(`precoCombo` if the KEY IS PRESENT else `preco`)** in integer centavos. ⚠️ `precoCombo` is **absent (not null)** on `internet` for every Fibra+TV card — a falsy-default read would drop the fibre component and under-price those three by R$99,90–179,90 (regression-tested).
  - ⚠️ **5 ghosts excluded:** the CMS lists combos SP doesn't sell; their catalog entry is `{"notFound": true}` (15 cards → 10 live). Names/GB/speed come from `catalog.nomeAutomatico` + `recursosDescritivos["602"]`, never the stale CMS text ("Controle 41GB" vs the priced 40GB). `acrescimoNaoDCC` R$5 on the entry tier → `payment_method="debit_auto"` + note. **SP is an explicit request parameter**, so segmentation can't drift with the egress IP.
- **Guards re-verified with THREE targets** (unchanged from #31): per-target `try/except` in `main.run()`, the merge/sheet-write `try/except` inside `write_workbook`, and the always-read-and-re-emit of `convergent_history`. A test proves one source raising still lets the other two collect and the mobile workbook write.
- **Live end-to-end:** **24 offers** (TIM 3 / Vivo 11 / Claro 10) written for 2026-08-03 with the mobile history **fully preserved** (2 336 rows / 42 snapshots; matrix still 42 rows, Post TIM 119,99 / Digital Vivo 45 / Claro 44,90).
- **Tests 120 → 132** (+12): both adapters against trimmed **shape-faithful** captures (real card markup / real catalog JSON), VIV-collision distinctness, hidden-price fallback, struck-through promo, SP-label detection, grid-by-component-name, ghost exclusion, centavos + absent-`precoCombo`, catalog-over-CMS fields, batched SP-pinned URL, three-carrier append + same-date idempotency, and the one-source-fails guard. Suite green.
- **Next (Architect):** phase 3 — the convergent comparison sheet (entry-level combo per carrier over time, the `comparison`-style view for combos).

### 2026-08-04 — CODE TASK #33: convergent phase 3 — the `convergent_comparison` matrix (Code)
- Closes the convergent arc (#31 domain → #32 all three carriers → **#33 the comparison view**). Presentation/derived only: **no adapter, scraping, mobile-schema, mobile-matrix or mobile-bake change**. CONTEXT → **v0.9.0** (§14 extended).
- **New derived column `bundle_type`** on `convergent_history`, computed from the existing service flags (no extra scraping): **`Fibre + Mobile` / `Fibre + TV` / `Fibre + Mobile + TV`**. Added to `CONVERGENT_COLUMNS` in schema order, and **backfilled** for rows written before it existed (`_backfill_bundle_type`, incl. Excel's True/1/"VERDADEIRO" boolean round-trips) — the same self-healing the mobile columns got in #18, otherwise every past date would render blank. A shape outside the canonical three (e.g. `Mobile + TV` with no fibre) gets a descriptive label and **its own column group** rather than being forced into an existing one.
- **`convergent_comparison` sheet** — mirrors the mobile matrix (§7): row 1 = bundle-type titles merged over their 3 carrier columns, row 2 = TIM|Vivo|Claro, col A = Date, data from row 3 with **one row per snapshot_date, earliest first, `dd-mmm`**, rebuilt each run so new days appear automatically. Every cell = a **live `_xlfn.MINIFS`** over `convergent_history` for that carrier + bundle_type on that EXACT date, with the **cached value baked** beside it (#21 mechanism extended via `value_map`). House header, gridlines off, **per-bundle-type-block green→yellow scale**, freeze `B3`, R$ format, purple tab, in-sheet note.
  - **PREREQUISITE CONFIRMED (not assumed):** `convergent_history.snapshot_date` is stored as **TEXT** (`data_type='s'`) exactly like the mobile history — so the locale-safe date construction `YEAR()&"-"&TEXT(MONTH(),"00")&"-"&TEXT(DAY(),"00")` is required, and is what the formulas use.
  - **Cheapest WITHIN a bundle type, not cheapest overall** (Bridge's choice): carriers sell very different things under one "combo" banner, so a single cheapest-combo column would compare unlike with unlike and flip meaning whenever a thinner-but-cheaper bundle launched.
  - **Blank = the carrier sold no combo of that type that day** — an honest gap, never a 0 (formula maps 0/no-match to `""`; the bake omits the coordinate).
- **BUNDLE-TYPE SPREAD from the real 2026-08-03 data (24 offers)** — as the task predicted, and **no fourth type appeared**: **TIM** Fibre+Mobile **3**, no TV combos · **Vivo** Fibre+Mobile **6** + Fibre+Mobile+TV **5** · **Claro** all three (**4 / 3 / 3**). First matrix row: Fibre+Mobile **TIM 89,99 · Vivo 160,00 · Claro 129,80** | Fibre+TV **Claro 219,80** only | Fibre+Mobile+TV **Vivo 190,00 · Claro 259,80** (TIM blank).
- **Formula == bake verified in real Excel:** a COM full recalc compared **all 18 cells** against the baked values → **0 mismatches**, and the blank cells stayed blank. `_conv_matrix_value` mirrors `_conv_minifs_day` exactly (the `_matrix_value` discipline).
- **Graceful degradation:** the matrix builds in its own `try/except` **nested inside** the convergent guard — a failure keeps the collected `convergent_history`, leaves `conv_values` empty (so the fail-loud bake check can't abort the run over a skipped side sheet), and never touches the mobile write. Covered by a test.
- **Mobile untouched:** 2 394 rows / **43 snapshots**, mobile matrix still 43 rows, Post TIM 119,99 / Digital Vivo 45. Sheet count 11 → 12.
- **Tests 132 → 143** (+11): bundle_type derivation (all three canonical + the no-fibre and single-service edges), row exposure + schema order, pre-#33 backfill, group list = canonical + any new shape, matrix layout/rows-per-date/earliest-first, exact-date MINIFS with the text-date construction, cheapest-within-type + blanks (a TV-bearing offer must not leak into the Fibre+Mobile column), **baked == helper for every cell**, CF blocks + freeze + note, header-only sheet when the history is empty, and the guard test. Suite green.
- **Next (Architect):** optional phase 4 — charts for the convergent matrix (the `Charts`-sheet treatment) and/or a convergent price-change alert. Also still open from #32: no adversarial review has been run over the two phase-2 adapters.

### 2026-08-04 — CODE TASK #34: convergent matrix — "Fiber" spelling + scoped to Fiber+Mobile (Code)
- Two small Bridge-directed changes on top of #33. **Presentation/label only** — no adapter, scraping, mobile-schema, mobile-matrix or mobile-bake change. CONTEXT → **v0.9.1** (§14).
- **(A) "Fibre" → "Fiber"** in every user-visible / stored `bundle_type` label: `Fiber + Mobile`, `Fiber + TV`, `Fiber + Mobile + TV`, plus the descriptive fallback for a non-canonical shape (e.g. `Fiber + Landline`). Constants renamed `BUNDLE_FIBER_*`.
  - **THE regression risk, handled:** #33's `_backfill_bundle_type` only filled BLANK values, so rows already stored as "Fibre + Mobile" would have survived the rename, stopped matching the matrix's MINIFS criteria, and **every past date would have silently gone blank**. `bundle_type` is a pure function of the service flags, so the merge now **RECOMPUTES it on every write** — one rule that fixes blanks *and* superseded spellings — with `migrate_bundle_type_label()` as the fallback when the flags are absent.
  - **Verified explicitly:** built a workbook, forced its `convergent_history` back to the legacy "Fibre …" spelling, then wrote again — labels migrated to "Fiber …", and the **past date still rendered 89,99 / 160,00 / 129,80** (the exact failure mode this guards against).
- **(B) The matrix now shows `Fiber + Mobile` ONLY** — the like-for-like headline bundle all three carriers sell — as the **cheapest such combo per carrier per date**. Single merged group title `Fiber + Mobile (R$/mo)` over TIM | Vivo | Claro; everything else from #33 kept (live exact-date `_xlfn.MINIFS` with the locale-safe text date, baked cached values, green→yellow CF over the block, freeze `B3`, gridlines off, house header, R$ format, tab colour).
  - Driven by **one constant, `COMPARISON_BUNDLE_TYPES`** in `convergent.py` — restoring a group is a one-line change; the sheet builder hard-codes no bundle-type literals.
  - **Collection unchanged and unfiltered:** all bundle types are still scraped and written to `convergent_history` (2026-08-03: Fiber+Mobile 13, Fiber+Mobile+TV 8, Fiber+TV 3). The in-sheet note now names the types on file but not charted, so the omission is visible rather than silent; `convergent_bundle_types()` reports what was collected and feeds that footnote.
- **Verification (regenerated offline from committed history):** sheet has exactly 4 columns (Date + 3 carriers), one group; **2026-08-03 row = TIM 89,99 · Vivo 160,00 · Claro 129,80**; **baked == helper for every cell**, and an **Excel COM full recalc matched all cells (0 mismatches)**. Vivo's R$190 Fiber+Mobile+TV and Claro's R$219,80 Fiber+TV correctly do NOT leak into the Fiber+Mobile column. Mobile untouched: **2 394 rows / 43 snapshots**, mobile matrix still 43 rows, Post TIM 119,99 / Digital Vivo 45.
- **Tests 143 → 145.** New/updated: Fiber-spelled derivation incl. the fallback, legacy-label migration (both with and without flags), ONLY-one-group layout (+ history still holds all types), cheapest-Fiber+Mobile with TV offers excluded and day-2 blanks, baked == helper, footnote names the uncharted types, header-only sheet when empty. Fixed a self-defeating assertion of my own while writing them: `ws.cell(1, 5)` *creates* the cell in openpyxl and inflated `max_column` before it was read — `max_column` is now read first.
- **Next (Architect):** optional phase 4 — charts for the convergent matrix and/or a convergent price-change alert. Still open from #32: no adversarial review over the two phase-2 adapters.

### 2026-08-05 — CODE TASK #35: full verification audit — data, matrices, alerts (Code)
- **READ-ONLY audit.** One polite SP pass over all 15 sources (mobile: claro 20 / tim 17 / vivo 26; convergent: TIM 3 / Vivo 11 / Claro 10 + 5 ghosts), captures saved, then **every capture re-parsed independently** (selectolax/raw JSON, not via the adapters) and compared to what we store. No parser/matrix/alert code changed. Full note: **`docs/source_audit_2026-08.md`**; CONTEXT → **v0.10.0**.
- **CONFIRMED CORRECT:** TIM prepaid validity **17/22/30** (#23) — the shared 30-day line is still there but no longer wins; **Vivo Lite** headline is the no-commitment price and the toggle **succeeded** (the 20GB card genuinely has no toggle, so its blank loyalty is right, not the #30 failure); **TIM postpaid `payment_method`** correct on all 7 plans (#24); **convergent: no drift** vs #32; **both matrices: history == formula == bake for EVERY cell** (recomputed in pandas + Excel COM recalc — zero disagreements); alert plumbing sound (rule, digest, secret-only-from-`EMAIL_APP_PASSWORD`, graceful degradation, can never block the commit). SP confirmed for all 12 mobile sources (TIM's evidence is weaker than #22 claimed — `rel=canonical` is state-less; the real gate is `field_regioes`).
- 🔴 **BLOCKER — the only price alert this project has ever sent was FALSE.** The 2026-08-03 "TIM Fit −42.9 % / −33.3 %" email was a **parser tier-substitution**, not a TIM price cut: TIM rebuilt `#price-fit` into **2 regional blocks × 2 tiers** (ids are now `modal-fit-anual-1.0-sp-sc-rn-ce` …), so (a) our first-match regex took a *different* "1.0" offer (`Tenha 25GB por 12x R$20`) while the tracked tier still reads R$30, and the R$35 Mensal **is still on the page** and is contradicted by its own modal; (b) `_fit_code`'s exact `modal-fit-anual/mensal` lookup found nothing, so `plan_id` degraded `tim:TIM202600000271 → tim:fit-anual` on that exact date, and the #29 name fallback then manufactured the "move". The Fit parser also has **no region gating** and captures only 1 of the 2 SP tiers.
- 🟠 **Vivo Controle: 7 of 8 headline prices include a PRE-CHECKED R$5 add-on** (80 vs base 75, 90 vs 85, 95 vs 90, 110 vs 105 — bases sit in `data-original-price`). This is also the **root cause of the alert noise**: `plan_id` embeds the parsed GB, so the same offer flips `…-61gb`↔`…-51gb` and emits ±5 % lines that reverse the next day.
- 🟠 **A Claro Controle card is silently dropped.** The control capture yields 4 plans but only 3 are stored: `claro:plano-flex-20gb` ("Controle 20GB", **R$44,90**) collides with the Flex row because `_merge_history` dedupes on `(snapshot_date, carrier, state, _key)` **without `category`**.
- **ALERT COVERAGE — definitive: MOBILE ONLY.** `alerts_from_workbook` hardcodes `sheet_name="history"`; `convergent_history` is never read and `compute_price_alerts` reads `plan_id`/`plan_name` which convergent rows don't have. **A Vivo Total combo going R$160 → R$200 sends no email**; the 21 convergent offers that appeared on 08-04 produced zero notification. New/removed plans are also uncovered (4 new Vivo postpaid plans today went unmentioned). Replaying all 43 day-pairs: **19/43 days (44 %) would email, 109 lines, 79 % of them the Vivo render flip.** Two structural risks in my #29 fallback: it can **pair two genuinely different plans** (176 non-unique name groups; demonstrated "Vivo Lite R$35 → R$75 +114 %") and it **silently skips** a synchronised same-name rotation.
- **FIRST adversarial review of the two phase-2 convergent adapters** (31 findings). Worst: 🔴 `vivo_convergent._price_from` treats every `.` as a thousands separator → **100× price** on a dot-decimal; 🟠 `_scroll_all` can return a **silently short** card list; 🟠 `offer_id` changes if the carrier edits the CTA deep-link (fabricating remove+add) and a DOM re-order can hand an id to a different offer; 🟠 Claro's ghost rule **absorbs catalog failures** (a live offer with a failing catalog entry is deleted as a "ghost") and `_PRICED_SLOTS` is a closed list that silently under-prices a combo if a slot is added/renamed.
- **Production status worth knowing:** CI is **12/12 green**, but the committed workbook holds only **3 TIM convergent rows** and **no `convergent_comparison` sheet** — #32–#34 all landed after the last daily run, so the first CI run with all three carriers + the matrix is the next scheduled one.
- **Next (Architect) — prioritized fix list (§7 of the note):** P0 TIM Fit parser (region-scoped modals, both SP tiers, restore etiqueta id, fix loyalty) + the Vivo 100× price bug; P1 Vivo Controle add-on price, `category` in the merge key, stable Vivo mobile `plan_id`, extend alerts to convergent + tighten the #29 fallback; P2 the remaining adapter hardening.

### 2026-08-05 — CODE TASK #36: P0 fixes — TIM Fit parser + the Vivo 100× price bug (Code)
- Scope limited to the **two P0 items** from the #35 audit's §7 list. The P1/P2 items (Vivo Controle add-on price, `category` in the merge key, stable Vivo `plan_id`, alert coverage, remaining hardening) are **untouched and still open**. CONTEXT → **v0.10.1**.
- **FIX 1 — TIM Fit parser rewritten** for TIM's ~2026-08-03 rebuild (2 regional blocks × 2 tier-versions × 2 plan types = 8 cards). Parsing is now anchored on each card's **`data-modal-open`** link, so a card can only be paired with **its own** price, name and etiqueta:
  - **Region gate = the modal id's region list** (`sp-sc-rn-ce` / `demais-ufs` catch-all). ⚠️ Reporting a deviation from the task brief: the Fit section carries **no `field_regioes`** — that field exists only on `ofertas` JSON nodes — so the region list is the only real gate the page offers. A non-SP state correctly falls through to `demais-ufs` (verified: MG gets the 20/30GB variants, SP the 25/35GB ones); blocks are never mixed.
  - **All four SP tiers captured** (was one per plan type, and the wrong one): `tim:TIM202600000306` Anual 1.0 25GB 12x R$20 · `tim:TIM202600000271` Anual 2.0 35GB 12x R$30 · `tim:TIM202600000270` Mensal 1.0 20GB R$20 · `tim:TIM202600000305` Mensal 2.0 35GB R$45.
  - **Stable etiqueta `plan_id` restored** — never the `tim:fit-*` name slug that the #29 fallback then mispaired.
  - **Loyalty:** `12x` on the card ⇒ annual credit-card plan (`loyalty_months=12`, `payment_method="credit_card"`), modelled as Vivo's annual is (#26) so the Digital pick still reads the no-commitment Mensal. ⚠️ **The page labels EVERY Fit etiqueta — annual included — "sem prazo de permanência"**; that wording is recorded verbatim in `price_note` so the modelling choice stays visible for the Architect to revisit.
  - **Card-vs-modal contradiction REPORTED, not resolved:** SP's Mensal 1.0 card shows **R$20** while its own modal still shows **R$35** (the demais-UFs card and modal both say R$35). We record what the SP visitor is shown and flag it in `price_note` — GOVERNANCE, never invent a number. **This one value remains genuinely ambiguous at the source.**
- **FIX 2 — `vivo_convergent._price_from` 100× bug.** It stripped every `.` as a thousands separator, so a dot-decimal `data-original-price="160.00"` became **16 000**. Now decided by pattern on the last separator: `1.200,00`/`24,99` → comma-decimal · `1,200.00`/`160.00` → dot-decimal · a lone separator with exactly 3 trailing digits (`1.200`) → thousands group. All 12 cases pass and the 11 captured Vivo combos parse **identically** (R$160 → R$1 200).
- **REPLAY — the false alert is gone.** The plan tracked as "Fit Anual" (etiqueta 271) read **R$30 on 2026-08-02** and reads **R$30** with the fixed parser → **no move**, so the −33.3 % signal does not reproduce. (The −42.9 % Mensal line is the ambiguous card-vs-modal value above, now flagged rather than asserted.)
- **FIRST POST-FIX RUN emits NO spurious Fit alert — verified live.** Both the ids and the names change (`tim:fit-anual` → `tim:TIM202600000271`, "…Fit Anual" → "…Fit Anual 2.0"), so the #29 fallback cannot pair them: the `changes` sheet shows **4 new + 2 removed** for tim/fit, and new/removed are not alerted (#35 finding). The live run's 3 alerts are the **genuine** TIM Controle cuts (Premium −17.6 %, Smart −15.3 %, Redes Sociais −7.7 %).
- **Live pass (one polite SP round):** claro 20 / **tim 19** (17 + the 2 extra Fit tiers) / vivo 26 = 65 plans; convergent unchanged at TIM 3 / Vivo 11 / Claro 10 (+5 ghosts). Mobile 2 458 rows / 44 snapshots; convergent 27 rows across all three carriers. History is append-only — the false 2026-08-03/04 R$20 rows **stay** (documented in the audit note), not rewritten.
- **Tests 145 → 163.** New `tests/test_tim_fit.py` (8 tests, fixture derived from the real rebuilt capture): both SP tiers per type, stable etiqueta id never a slug, the first-match-wrong-offer regression pinned, region gate + `demais-ufs` fallback, loyalty attribution, card-vs-modal reporting, empty-section. Plus 10 parametrised BRL cases for the Vivo parser. **Two #28 tests were RETIRED** — they pinned the old single-`modal-fit-anual` markup that no longer exists on the page; their intent is covered by the new module against real markup.
- **Next (Architect):** the P1 list from `docs/source_audit_2026-08.md` §7 — Vivo Controle add-on price (also removes ~79 % of alert noise), `category` in the `_merge_history` dedupe key, stable Vivo mobile `plan_id`, and extending alerts to convergent + tightening the #29 fallback.

### 2026-08-05 — CODE TASK #37: P1 data fixes + convergent alerts + sanity guardrail + missed-run protection (Code)

**This closes the build phase.** All four 🟠 P1 audit findings are resolved and the job is hardened for unattended operation. CONTEXT → **v0.11.0**. Scope held to Parts A–F; the 🟡 P2 list stays open and optional.

**A — Vivo Controle now stores the BASE price.** Every Controle card ships a **pre-checked** "+10 GB · R$5" switch (`data-startdisabled="checked"`), so `.total-card-price-value` was showing base+add-on and **7 of 8 headlines were wrong**. The base lives in `data-original-price` and is only trusted when the switch really is pre-checked **and** the attribute is lower than the displayed figure. Live SP 2026-08-05, base → (screen):
`Vivo Controle 59` (no add-on, 46 GB) · `Vivo Controle **75** (80)` · `Educação **85** (90)` · `Música **85** (90)` · `Saúde **85** (90)` · `Youtube Lite **85** (90)` · `Netflix **90** (95)` · `Entretenimento **105** (110)`. The screen figure is kept in `price_note`, never as the price. The **mirrored `lite` rule is untouched** (there the *displayed* value is the no-commitment headline and the attribute is the 12-month loyalty price) and both directions are pinned by tests — inverting it would have violated the standing no-loyalty-headline rule.

**B — stable Vivo `plan_id`.** Key chosen: **`vivo:{VIV-offer-code}`** — carrier-native, unique per card (verified: 8/3/11 distinct codes for 8/3/11 control/lite/postpaid cards), **no parsed GB and nothing price-derived**. Prepaid keeps a **validity** token (`vivo:VIV…-30d`) because its tiers share one page-level code; validity is an attribute, not a price.
- **Replay evidence:** `VIV202600030938` (Controle Netflix) sits in history as `…-61gb` (15 days @ R$95) **and** `…-51gb` (7 days @ R$90) — one plan, two identities, two "prices". Under the new key it is one id.
- ⚠️ **Honest reporting:** A and B act at **capture** time and history is append-only, so a replay over stored rows **cannot** show the improvement. I tried a simulation that rewrote stored ids and **discarded it** — a blanket `-NNgb` strip also collapses Claro's *native* `…-25gb`/`…-30gb` slugs (genuinely different plans) and manufactured **540 fake moves**. Scoped correctly to Vivo it produces *more* lines, not fewer, and that is the correct result: with a stable id the old flipping **prices** match directly instead of being suppressed by the fallback. What flipped was the price, and that is Part A. **Measured: 79 of 79 stored Vivo-Controle alert lines are an exact ±R$5 move — the add-on's fingerprint. 100 % of Vivo-Controle noise, 77 % of every alert line ever produced.**

**C — `category` is part of a plan's identity.** `_merge_history` deduped without it, and Claro's controle page **reuses the flex slug**, so `claro:plano-flex-20gb` = "Controle 20GB" **R$44,90** collided with "Flex 20GB" R$59,90 and one was dropped.
- **Before → after (2026-08-03 → 2026-08-05, live):** claro control **3 → 4** (R$44,90 recovered), claro flex 4 → 4, claro postpaid 5 → 5, claro prepaid 7 → 7, tim control 5 → 5, tim fit 2 → 4 (#36's extra tiers), tim postpaid 7 → 7, tim prepaid 3 → 3, vivo control 8 → 8, vivo lite 3 → 3, vivo postpaid 7 → 11 (4 genuinely new Vivo cards), vivo prepaid 4 → 4. Totals **58 → 65**. Nothing lost, nothing duplicated. The Claro **Control** matrix entry correctly drops **59,90 → 44,90**.
- 🔬 **Found by the live replay, not by the tests — and worth remembering.** Once both rows survive, one snapshot legitimately holds the **same `plan_id` twice**, and the alert matcher still keyed on `(carrier, state, plan_id)`: the two collapsed and the R$15 gap emitted a **false −25 % cut**. `category` had to go into **all three** keys together (`_merge_history`, `_compute_changes._ck`, `alerts._key`). **Fixing the dedupe alone is worse than the original bug** — it converts a missing row into a false alert. (Adding the 4th tuple element also broke a positional read, `k[2]` → `k[-1]`, which silently disabled *every* convergent alert until caught by the suite.)

**D — alerts cover CONVERGENT, in one digest, with a stricter fallback.** The ≥3 % rule is now one parameterised function; mobile reads `plan_id`/`plan_name`, combos read `offer_id`/`offer_name`; `alerts_from_workbook` reads **both** sheets and emits **one** e-mail with **`MOBILE PLANS`** / **`CONVERGENT (COMBO) OFFERS`** sections (a section with no lines is omitted). All graceful-degradation guarantees kept — **alerts still never block the scrape or the commit**. The #29 fallback now refuses to pair unless the name is unique in the **full snapshot on both sides** (was: unique only among unmatched rows — the audit's 176 non-unique name groups and the false "+114 %"); re-keys print `[re-keyed old → new]`.
- **Replay, 43 day-pairs: 19/43 days and 102 lines** (audit baseline 19/43, 109), false Claro −25 % gone. That is the ceiling for a *matching* change against already-written rows; the noise is a capture problem (see B).
- **Verified the combo path fires:** a stubbed R$129,80 → R$149,80 Claro Multi move produces a digest line; a real two-day workbook yields one mobile + one convergent line in a single e-mail.
- **Not taken (deliberate):** the audit's "add new/removed lines to the digest" sub-item. New/removed are not price moves, and every carrier id rotation would spam them; the `changes` sheet already carries them.

**E — sanity guardrail + honest retry.** Bands in `config/sources.yaml` (mobile claro 12–32 / vivo 16–38 / tim 12–30 against live 20/26/19; convergent tim 2–10 / vivo 6–20 / claro 5–20 against live 3/11/10) and price bounds **R$5–2000** — the ceiling is 2000 *because Vivo's V 1 Giga combo is genuinely R$1.200*; 1000 would band out real data daily. `check_sanity` is pure and judges both domains, only for carriers actually attempted (so `--only` still works). **Enforcement returns 4 BEFORE `write_workbook`**, so a degraded run neither commits nor overwrites the good workbook, and e-mails `SANITY CHECK FAILED` (guarded — a failing e-mail cannot un-block anything). The old zero-guard is subsumed **and improved**: it used to return 3 *after* writing.
- **It fired for real during this task.** A mis-wired replay harness fed the wrong page to Claro and TIM; the guard reported `count_low` for both, exited 4, and left the committed workbook untouched. That is exactly the intended behaviour, observed rather than asserted.
- **Retry:** `fetch_with_retry`/`is_transient_error` now back **all six** adapters. Timeouts / resets / `net::ERR_*` retry with backoff; a **403/CAPTCHA is surfaced immediately**. The previous per-adapter loops retried *any* `HTTPError` — i.e. they hit a refusing site twice, against GOVERNANCE §3.

**F — the lost day, diagnosed and defended.**
- **F1 — what actually happened on 2026-08-04 (from the run log, not inference):** the scheduled run **did fire** (`30955188476`, 22:07:50Z) and **failed after 1m17s in the "Run tests (offline)" step, which runs BEFORE the scrape** — `tests/test_convergent_phase2.py::test_one_convergent_source_failing_does_not_stop_the_others_or_mobile — assert 3 == 0` (`1 failed, 144 passed`). That test, written in #32, stubbed **only `tim`**, so vivo/claro yielded 0 plans and the committed `main.py` zero-guard returned 3; it passed locally only because uncommitted work had replaced that guard. **The job never reached the scrape.** Not a GitHub problem: the workflow is `active`, the cron fired on time, and 2026-07-18 → 2026-08-03 were all green. **Last successful scheduled commit: 2026-08-03.**
- **Complete gap list — `history`: 44 snapshots over 45 days (2026-06-22 → 2026-08-05), missing exactly `2026-08-04`, 98 % coverage. `convergent_history`: 2 of 3 days, missing the same date.** No other gap in either sheet. **2026-08-04 is unrecoverable and is NOT backfilled** — carrier pages show only current prices.
- **Job health: healthy.** The offending test is fixed (all three carriers stubbed, no `only=`, guard-agnostic) and the full suite is green, so tonight's 21:00 UTC run should produce the first #37 snapshot.
- **F2 — staleness alert** at the start of every live run: newest snapshot older than `sanity.max_snapshot_age_days` (default 2) → `STALE DATA` e-mail naming the age **and every missing date**. Guarded, never blocks. Verified: 4-day-old history lists 08-03…08-06; fresh history is silent; a missing workbook cannot crash it.
- **F3 — watchdog workflow** `.github/workflows/mobile-price-tracker-watchdog.yml`, 23:00 UTC (**20:00 BRT**, ~2 h after the scrape): reads the **committed** workbook, e-mails `NO SNAPSHOT TODAY` when today is absent. **No scraping, no writing** — `permissions: contents: read`, separate concurrency group, 10-min timeout, only pandas/openpyxl/pyyaml (no Playwright). **Always exits 0** — it reports, it never fails a workflow. Verified: silent when today is present; one e-mail naming today and the newest-on-file when absent; an unreadable workbook also e-mails; an SMTP failure is not fatal.
- ⚠️ **The lesson, recorded because it will matter again:** *a test that gates the scrape is a data-loss risk, not just a red X.* The workflow treats a CI failure and a scrape failure identically, but here they cost very different things. F2/F3 **detect** this class of failure; they do not prevent it. Any test asserting a specific exit code, or leaning on the sanity bands, can kill a day of data when the world drifts.

**Verification across the task**
- **No second scrape.** A full live SP pass had already run today (14:00–15:01, all 12 mobile + 3 convergent captures), so #37 was verified by **replaying those captures through the real pipeline** — same parsers, same guardrail, same alert logic, same workbook write, network stubbed only at the transport boundary. GOVERNANCE §3 allows one pass a day.
  - ⚠️ **Self-inflicted, reported in full:** the first replay harness patched `httpx.get` per-module, but `tim.httpx`, `claro.httpx` and the convergent modules are the **same module object**, so every TIM target received the Ultracombo page — and `_save_raw` then **overwrote all four of today's real TIM captures** with it (confirmed: four files, one MD5). Repaired with **three** targeted GETs (the `controle` page backs both `control` and `fit`), and the harness now stubs `_save_raw` so a replay can never write to `data/raw/` again.
- **Both matrices consistent:** `comparison` (13 cols × 44 date rows) and `convergent_comparison` (4 cols × 2 rows) — **history == python helper == baked cached value == live Excel recalculation** (`CalculateFullRebuild` via COM), **534 cells, 0 mismatches**. 2026-08-05 row: Control TIM 49,99 / Vivo 59 / Claro **44,90** · Post 119,99 / 150 / 124,90 · Pre 30 / 30 / 30 · Digital 20 / 45 / 44,90; combos TIM 89,99 / Vivo 160 / Claro 129,80.
- **Counts:** mobile **65 plans** (claro 20 / tim 19 / vivo 26) over **44 snapshots**; convergent **24 offers** (tim 3 / vivo 11 / claro 10) → **27 rows** over 2 snapshots.
- **Tests 163 → 203** (`tests/test_task37.py`, 40 new). Covers: add-on base price + the GB de-bundling and the two cases where it must decline + the three ways the rule must decline + the lite rule intact; stable id across two captures with different GB, not price-derived, prepaid validity token; the category dedupe collision and that dedupe still works; the reused-id-across-categories false alert (both the digest and the `changes` sheet); convergent alerts, the merged digest and its section ordering/omission; the tightened fallback refusing and still firing for a real rotation; sanity bands/prices/the R$1.200 combo/`--only`/blocking-with-no-write; retry transient-vs-block-vs-budget; staleness fresh/stale/missing-file; the watchdog's four paths; e-mail degradation. **Every new test was mutation-checked** — each fix was reverted in turn and the pinning test confirmed to fail (5/5 caught), so none is vacuous.
**Adversarial review of the #37 diff — 5 lenses, every finding independently refuted before acceptance. 3 confirmed, 14 refuted; all 3 fixed here.**
1. 🔴 **The add-on's +10 GB had to come off with its R$5.** Part A de-bundled the price but not the data: `.unique-card__header-benefit` reports the add-on-**inclusive** allowance (46 GB @ R$59 unchecked vs 61 GB over a R$75 base), so rows read **R$75 / 61 GB** — a package Vivo does not sell (61 GB costs R$80), understating R$/GB by ~16 % on 7 of 8 plans and mislabelling the Ranking sheet. `_addon_gb()` now reads the switch's own **`data-gb-bonus`** (base64 `"MTA="` → 10, beside `data-price-bonus` `"NQ=="` → 5) with the "+ 10 GB" label as fallback, and only subtracts when both numbers are readable and the result stays positive. Stored now: **75 / 51 GB**, and 51 = 20 franquia + 31 bônus — an exact match to `data_note`.
2. 🔴 **`_rotation_pairs` had been left on the #29 rule** while `alerts.py` was tightened, so the `changes` sheet would have reported a `price_change` the e-mail refused to send — breaking the §3 promise that the two agree. Both now require full-snapshot name uniqueness, with a test that feeds the same input to both.
3. 🟠 **The convergent digest could repeat itself forever.** The convergent pass is guarded and legitimately collects nothing, in which case `_merge_convergent` adds no new date; diffing each sheet's own two latest dates would re-send yesterday's combo moves **stamped today, every day**. Now gated on `conv_date == mob_date`, with a log line when it skips.
- **Bonus hazard closed (also from the review):** the workflow exposed `mode: demo`, and `--demo` writes the adapters' **hardcoded sample prices** with every output guard skipped — a demo dispatch would have **committed fabricated numbers into the append-only history**. The commit + weekly-backup steps are now gated on `mode == 'live'`.
- Everything else the reviewers filed (14 items) was refuted with evidence — e.g. the watchdog's UTC-vs-BRT date, `config.load()` being unguarded, the prepaid id fallback, and the `max_snapshot_age_days: 2` tolerance (covered by F3).

⚠️ **TWO CAPTURE-CORRUPTION INCIDENTS, reported in full — `data/raw/` is the audit trail and tooling must not write to it.**
1. My first replay harness patched `httpx.get` **per module**, but `tim.httpx`, `claro.httpx` and the convergent modules are the **same module object** — the last patch won, every TIM target got the Ultracombo page, and `_save_raw` overwrote all four real TIM captures with it (four files, one MD5).
2. A review subagent exercised the live Vivo Total adapter, hit an error page, and `_save_raw` replaced the 1.2 MB capture with **147 bytes**.
Both were repaired with the **minimum** targeted re-fetch (3 TIM pages — `controle` backs both `control` and `fit` — and 1 Vivo render), and **both were caught by the sanity guardrail refusing the degraded run** rather than by me noticing. The harness now stubs `_save_raw`. Total extra requests today: 4, all repairs, all with the configured delays.

- **Review copy** regenerated locally and inspected, then `git checkout`'d — **`data/mobile_plans.xlsx` is not committed**; tonight's CI run regenerates it. `data/raw/` and secrets stay out of git as always.
- **Next (Architect):** nothing required — the project is in **steady-state operation**. Optional, whenever wanted: the 🟡 P2 hardening list in `docs/source_audit_2026-08.md` §7 (items 7–9; "don't retry 4xx" was resolved here as a side effect), convergent charts, or a R$/GB value lens.

### 2026-08-06 — CODE TASK #38: the test suite no longer gates data collection (Code) — **CLOSES THE BUILD PHASE**

**A second day was lost before this landed.** #37 recorded the lesson from 2026-08-04 but only *detected* the problem; it did not remove the dependency. The very same stale test then killed the **2026-08-05** scheduled run too — `31051578644`, `1 failed, 162 passed`, i.e. the pre-#37 tree, because the #37 push landed after 22:08 UTC. **Two calendar days of prices, gone to red unit tests that had nothing to do with scraping.** CONTEXT → **v0.12.0**.

**A — the decoupling.**
- **Two INDEPENDENT jobs, `tests` and `collect`, with NO `needs:` between them.** They start together; neither can affect the other.
- **`needs: [tests]` + `if: always()` was considered and rejected.** It still makes collection *wait* for the suite, so a hung, queued or cancelled tests job delays or skips the scrape. The requirement is that collection has **no dependency on the suite at all**, not a weakened one.
- **Still loud:** the `tests` job fails visibly (GitHub notifies) *and* sends a **`CI TESTS FAILING`** e-mail over the existing secret-based path, `continue-on-error` so mail trouble can't mask the real failure. The body states plainly that collection is unaffected and the data is safe — a red test must not read as an emergency, or it will be ignored.
- **Least privilege:** `tests` → `contents: read`, only `collect` → `contents: write`. `tests` also drops the Playwright browser download (no test launches a browser).
- **The stale #32 test corrected — and it was worse than "asserts the wrong exit code".** It replaced the entire `alerts_mod` with a stub class defining only `check_sanity`, so main.py's `check_staleness` call raised `AttributeError` into a `try/except` and **the test passed by accident**; worse, the run still reached the closing price digest, which re-imports the *real* alerts module (`from . import alerts as _alerts`) and therefore read the **real committed workbook** and entered the **real SMTP path**. The stubs now patch the functions on the real module, so the late re-import sees them too, and `send_alert_email` is stubbed to `pytest.fail` — no test can touch `data/` or attempt mail.

**B — verification, all empirical.**
1. **A red test no longer blocks collection — PROVEN.** Scratch branch `ci/prove-test-decoupling` with one deliberately failing test, dispatched live (run `31059163099`): **`tests: failure`, `collect: success`** in the same run. `collect` scraped **claro 20 / tim 19 / vivo 26 = 65 plans** and **tim 3 / vivo 11 / claro 10 = 24 combos**, passed the sanity guardrail, and committed `chore(data): daily snapshot 2026-08-06`. That snapshot was **cherry-picked onto `main`** so the day's real data wasn't thrown away with the branch; both scratch branches were then deleted (local + remote) and `main` carries neither the failing test nor any temporary edit.
2. **A bad scrape still blocks — PROVEN, not assumed.** Scratch branch `ci/prove-bad-scrape-blocks` replaced the tracker command with `exit 4` (simulating the sanity block; **no scraping at all**). Result (run `31060812862`): `Run tracker` **failure** → `Commit daily snapshot` **skipped**, `Weekly backup` **skipped**. This settles the open question from #37: a custom `if:` expression does **not** displace GitHub's implicit `success()`, so the demo gating never weakened the data guard.
3. **Demo gating still holds.** A `mode: demo` dispatch on `main` (run `31060687765`): tracker ran, **commit and backup both skipped**, whole run green — and it doubles as a full green run of the restructured workflow on `main`.
4. **The staleness alert and the watchdog both fired for real, on the genuine gap.** The live collect logged `alerts: stale-data email sent` (newest was 2026-08-03, 3 days > the 2-day limit). Independently, the scheduled watchdog run `31058404373` logged `watchdog: NO snapshot for 2026-08-06 (newest on file: 2026-08-03)` → `alert email sent`, exit 0. Neither needed prompting.
5. **Both matrices verified on the CI-produced workbook:** `comparison` 44 date rows × 12 + `convergent_comparison` 2 × 3 = **534 baked cells, 0 mismatches** against the python helper (= the formula), 48 legitimately blank. 2026-08-06: Control TIM 49,99 / Vivo 59 / Claro **44,90** · Post 119,99 / 150 / 124,90 · Pre 30 / 30 / 30 · Digital 20 / 45 / 44,90; combos TIM 89,99 / Vivo 160 / Claro 129,80. All 12 sheets present including `convergent_comparison`.

**⚠️ Found and fixed in passing — the watchdog's own date.** GitHub cron is best-effort: the 23:00 UTC watchdog actually started at **00:02 UTC**, past midnight, so `date.today()` asked "is there a snapshot for *tomorrow*?" — unanswerable, and it would cry wolf on every slow queue. The workflow now evaluates the UTC date **as of two hours ago** (correct on time, correct for delays up to ~3 h). Re-dispatched to confirm: at 00:44 UTC it evaluated **2026-08-05** and correctly reported that day missing. Workflow-only change; `watchdog.py` already accepted an explicit `today`.

**State as it stood at the end of this task — ⚠️ SUPERSEDED a few hours later by the timezone fix in the next entry. Read that one for the current numbers.**
- `history` **2 459 rows / 44 snapshots**, 2026-06-22 → 2026-08-06, **coverage 44/46 = 96 %**. `convergent_history` **27 rows / 2 snapshots**, 2026-08-03 → 2026-08-06.
- **Missing, in both sheets: `2026-08-04` and `2026-08-05`.** Both looked unrecoverable and were NOT backfilled; the genuine 08-05 raw captures sitting in the local, uncommitted `data/raw/` were flagged to the Architect rather than used, since writing them under an 08-05 date would still be backfilling a missing date.
- **What that analysis missed** (found immediately afterwards): the `2026-08-06` snapshot was itself misdated — it was scraped at 21:18 **BRT on 08-05** and only looked like the 6th because the runner stamps UTC. So 08-05 was never a lost day at all; it just needed the right date and a same-day re-scrape. **Final: coverage 44/45 = 98 %, gap = 2026-08-04 alone.**
- Scheduled runs: 07-30, 07-31, 08-01, 08-02, 08-03 green → **08-04 and 08-05 failed (the stale test)** → fixed. Both workflows `active`.
- **pytest: 203 passed.**
- **This closes the build phase.** Steady state: daily scrape 18:00 BRT, watchdog 20:00 BRT, weekly Monday backup. Open and **explicitly optional**: the 🟡 P2 list in `docs/source_audit_2026-08.md` §7 (items 7–9), convergent charts, a R$/GB value lens.
- **One thing worth the Architect knowing:** the trade this task makes is deliberate — a red suite now means untested code keeps collecting and committing. That is the right side to err on here, because a wrong parser is fixable from the raw captures and the append-only history, while a missing day never is.

### 2026-08-05 (22:59 BRT) — #38 follow-up: `snapshot_date` is the São Paulo day + today collected (Code)

**Bridge asked to "run for today". Investigating first showed the data was already collected — and surfaced a real bug instead.** CONTEXT → **v0.12.1**.

- **What was actually wrong.** The 21:16 BRT run earlier this evening had collected today's 65 plans + 24 combos, but filed them as **`2026-08-06`**. `config/sources.yaml` has declared `timezone: America/Sao_Paulo` since day one, yet `main.py` stamped `run_ts = datetime.now()` — on a **UTC** CI runner that is the UTC day. The daily cron (21:00 UTC == 18:00 BRT) agrees in both zones, so this hid for **44 consecutive snapshots**; only a run after 21:00 BRT (= past midnight UTC) exposes it. A future-dated row also poisons `latest`, `changes` and both matrices' newest column.
- **Fix:** `main.project_now(settings)` = `datetime.now(ZoneInfo(settings.timezone))` with tzinfo **stripped**, so `snapshot_ts` keeps the exact naive format all 2 400+ stored rows use (the column always meant local wall clock). `Settings.timezone` reads `project.timezone`, default `America/Sao_Paulo`. An unusable tz name logs and falls back to `datetime.now()` — **a clock detail must never be why a day's prices are lost.**
- **The misdated snapshot was corrected, then today re-scraped.** The 65 + 24 rows filed as `2026-08-06` were removed and the day collected fresh, landing as **`2026-08-05`** — a *mislabel correction plus normal same-day collection*, not a backfill: the scrape happened at 22:59 BRT on 2026-08-05, inside the day it records.
- ⚠️ **Learned the hard way:** dropping a date needs a **fresh output file**. `write_workbook` merges with whatever is on disk and `_merge_history` only replaces the dates present in the *incoming* set — so writing the filtered rows over the existing workbook left `2026-08-06` untouched (observed on the first attempt). Fixed by writing to a new path and `os.replace`-ing it in.
- **Live result:** claro 20 / tim 19 / vivo 26 = **65 plans**; tim 3 / vivo 11 / claro 10 = **24 combos**; sanity guardrail passed; both matrices **534 baked cells, 0 mismatches**. 2026-08-05 row: Control TIM 49,99 / Vivo 59 / Claro 44,90 · Post 119,99 / 150 / 124,90 · Pre 30 / 30 / 30 · Digital 20 / 45 / 44,90; combos TIM 89,99 / Vivo 160 / Claro 129,80.
- **Coverage improved: `history` 44 snapshots over 45 days (2026-06-22 → 2026-08-05) = 98 %. The permanent gap is 2026-08-04 ALONE** — 2026-08-05 is genuine, correctly-dated, freshly-scraped data.
- **Tests 203 → 205** (the São Paulo stamp; the fallback on a bad tz name). All green.
- ⚠️ **Deliberate exception, flagged:** this commit includes `data/mobile_plans.xlsx`, which Code normally never commits. Two reasons — the misdated row had to be removed at source, and CI could never have produced an 08-05 row (its next run stamps 08-06). Normal service resumes tomorrow: CI owns the workbook again.

### 2026-08-05 — CODE TASK #39: Claro's Control column was showing a Flex price (Code)

**Bridge, reading the comparison sheet: "Claro Controle has the same price as Claro Flex — it's double counting Flex."** Exactly right. CONTEXT → **v0.12.2**.

- **Mechanism.** For Claro, only `plan_id` is genuinely the carrier's — it is the modal's accordion slug. **`category` and `plan_name` are inherited from the PAGE being parsed**: `parse_next_data` builds the name as `f"{category_label} {gb}GB"` whenever the modal title is the generic "Mais detalhes". Claro **cross-sells `plano-flex-20gb` on the controle page at R$44,90** — the flex page prices that same slug at **R$59,90** — so our parser christened it **"Controle 20GB", category `control`, R$44,90**.
- **Damage.** R$44,90 undercut every genuine Controle plan (the real cheapest is **Controle 25GB R$59,90**), so **Control/claro read 44,90 — a Flex price — and Digital/claro read 44,90 too** (Flex 15GB). The same R$44,90 was counted in two columns, and Claro's Controle line looked R$15 cheaper than it is.
- **Fix.** `slug_line()` maps Claro's own slug prefix to a product line (`plano-controle-*` → control, `plano-flex-*` → flex, `plano-pos-*` → postpaid). A card whose declared line disagrees with the page it sits on is **skipped and logged** — its own page stays authoritative, so the plan is still tracked, just once. An unrecognised slug shape is **never** filtered: fail open rather than silently drop.
- **Dates adjusted.** Swept all **49** snapshots for any Claro row whose slug-line ≠ its stored category: **6 rows, on 2026-08-05, 08-06, 08-07, 08-08, 08-09 and 08-10** — i.e. every snapshot taken since #37 landed. All removed. Earlier dates were already clean because the pre-#37 dedupe (which lacked `category`) hid the row entirely. **Each corrected date now reads Control 59,90 / Digital 44,90, identical in shape to the 43 earlier ones.** Mobile **2 778 rows / 49 snapshots** (2026-06-22 → 2026-08-10); latest 64 plans (claro 19 / tim 19 / vivo 26); convergent 165 rows.
- **Verified:** both matrices baked == helper == **live Excel recalculation**, **609 cells**, 0 mismatches. Full suite **206 tests**, green.
- **Corrects the #37 record.** #37/§6 claimed this row was "a real Claro Controle plan silently dropped by the dedupe" — that came from the #35 audit and was wrong. The **dedupe fix itself stands** (a native id is only unique within a category page, which is true and still matters); the row was miscategorised further upstream, in the adapter. CONTEXT §6 now says so.
- **The general lesson, worth carrying:** *a page is a place, not a fact.* Category, name, product line — anything inherited from the page we happened to fetch is an assumption. Where the carrier states the fact itself (a slug, an etiqueta, `field_regioes`, an explicit `state=SP` parameter), trust that instead.
- ⚠️ Includes `data/mobile_plans.xlsx` again, same exception as the previous entry: the bad row had to come out at source. CI owns the workbook from the next scheduled run.


### 2026-08-19 — CODE TASK #40: a blocked carrier must not cost the whole day (Code)

**Trigger: the 2026-08-18 run failed and that day is gone.** CONTEXT → **v0.13.0**.

- **What happened.** TIM returned **403 Forbidden to the GitHub runner on all five of its URLs** (4 mobile targets + Ultracombo). Claro scraped 19 plans and Vivo 26, both clean. `tim: 0` tripped `count_low`, the sanity guardrail blocked the commit (exit 4), and **all three carriers' data was thrown away**. The `tests` job was green; the scrape itself was the failure.
- **Diagnosed, not guessed:** the identical request with the same User-Agent from Rafael's machine returned **200 (849 KB)** the same evening, and again the next day. So TIM is refusing **datacenter/Azure IP ranges**, which is where GitHub-hosted runners live. Nothing in our code broke, and a 403 is a refusal — correctly never retried inside the run (GOVERNANCE §3).
- **The actual defect was ours, though:** the guardrail could not distinguish *"this carrier never answered"* from *"this carrier answered with junk"*. Both looked like `count_low`. The first is a **gap**; only the second is degraded data worth blocking a commit over.
- **Fix 1 — unavailable ≠ degraded.** `main.run` now counts, per carrier, targets attempted vs targets that failed to fetch. A carrier where *every* target raised is `unavailable`, is excluded from the count bands, and gets its own **`CARRIER UNAVAILABLE`** e-mail. The carriers that answered are committed. A carrier that answers with 3 plans instead of 19 still blocks — pinned by its own test.
- **Fix 2 — convergent can no longer block mobile.** Surfaced by the new end-to-end test, not by reading code: #37 routed convergent count bands into the same blocking exit, silently contradicting §14's promise that a combo problem can never cost the mobile snapshot. `SanityIssue` now carries a `domain`; convergent issues drop that run's combo rows (stored convergent history preserved via the empty-list path) and e-mail `CONVERGENT DATA SKIPPED`, while mobile commits.
- **Fix 3 — catch-up pass.** Second cron at 00:00 UTC (21:00 BRT — still the same São Paulo day) running `--only-if-incomplete`: it reads today's snapshot, scrapes **only** the carriers missing from it, and does nothing at all when the day is complete. A healthy day therefore costs zero extra requests and no source is hit twice; a blocked carrier gets exactly one more chance, hours later, at a different time of day.
- **Proof, not assumption.** New test replays 2026-08-18 through `main.run()` with TIM raising 403 and Claro/Vivo healthy: **rc 0, workbook written with claro+vivo only, `CARRIER UNAVAILABLE` sent, no `SANITY` mail.** Mutation-checked — reverting either guard makes the suite fail. Tests 206 → **212**, all green.
- **2026-08-18 stays missing.** Carrier pages show only current prices; it is never backfilled. Gaps to date: **2026-08-04, 2026-08-15, 2026-08-18**.
- **Next (Architect):** watch whether TIM's 403 persists from CI. If it becomes permanent, the remaining options are a scheduled local pass for TIM only (gives up "PC can be off" for that carrier) or accepting TIM as a recurring gap — both are Bridge decisions, not Code's.


### 2026-08-19 — CODE TASK #41: stop patching causes, make a lost day structurally hard (Code)

**Bridge: "we can't be adjusting this every day."** Correct — three losses, three point-fixes, and the next unknown fault would have cost a fourth day. CONTEXT → **v0.14.0**.

- **The pattern I had been missing.** 08-04 (red unit test), 08-15 (6-hour dependency hang), 08-18 (carrier 403) look unrelated, and I fixed each cause separately. They share one structure: **a day had exactly ONE chance to be collected.** Any fault in that window — including classes we have not met — takes the day permanently, because carrier pages only show current prices. Patching causes one at a time can never converge; adding chances does.
- **Four passes per São Paulo day:** 18:00 BRT (main) then 20:00, 22:00, 23:30. Every pass after the first runs `--only-if-incomplete` — it reads today's snapshot, scrapes ONLY carriers missing from it, and exits immediately when the day is complete. **A healthy day still hits each source exactly once**, so the politeness rule is untouched; the extra crons cost ~20 s each. The shell dispatcher is inverted (everything except the main cron is a catch-up), so a future schedule can't be added and silently behave as a full re-scrape.
- ⚠️ **The catch-up was destructive in its first form, and only a test found it.** `_merge_history` replaces every row for a `snapshot_date` when that date is written. A pass that scraped only the missing carrier would therefore have **deleted the carriers the earlier pass had collected** — the rescue mechanism actively causing the loss it exists to prevent. `stored_plans_for()` now re-submits the rows the day already holds (rows collected today, for today — not carried across days). Mutation-checked: stub the carry-forward and the suite fails.
- **Fail fast.** `collect` had **no `timeout-minutes` at all** — it inherited GitHub's 6-hour default, which is exactly how 08-15 consumed the entire day and ~360 private-repo Actions minutes on a stalled Ubuntu mirror. I diagnosed that in passing and never fixed it; it is fixed now. `collect` 25 min, `tests` 15 min, install step 12 min, with `playwright install --with-deps` bounded by `timeout` and retried 3×, then falling back to a browser-only install that skips the apt step that actually hangs. Browser is cached, so a normal run downloads nothing.
- **Watchdog moved to 00:30 BRT** — after the last catch-up, since asking "did today land?" before the retries have run would cry wolf — and corrected to judge the **São Paulo** day. It was still anchoring to UTC, which has been wrong since #38 made `snapshot_date` a São Paulo date; the two agreed by luck at its old hour and would have diverged at the new one.
- **Tests 212 → 216**, all green. Every new guard mutation-checked — which caught one of my own tests being vacuous: the convergent loop swallows per-source exceptions by design, so a test that "proved" no re-scrape by raising would have passed with the guard removed. It records calls now.
- **What this does NOT fix, stated plainly:** if a carrier refuses us for an entire day, that carrier is still a gap for that day (the other two commit — #40). Four passes make a *transient* fault survivable; they cannot defeat a sustained block, and defeating one covertly is against GOVERNANCE §3 anyway. Gaps to date: **2026-08-04, 2026-08-15, 2026-08-18**.
