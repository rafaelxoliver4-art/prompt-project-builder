# Mobile Price Tracker dependencies
pyyaml>=6.0
httpx>=0.27           # fast plain HTTP for non-JS pages / JSON endpoints
selectolax>=0.3       # fast HTML parsing (lxml-based)
beautifulsoup4>=4.12  # fallback / convenient HTML parsing
pandas>=2.2           # dataframes + Excel IO
openpyxl>=3.1         # Excel read/write, formatting, formulas
playwright>=1.44      # headless browser fallback for JS-rendered pages
# After install, Code must run once: python -m playwright install chromium

# Dev/test
pytest>=8.0
